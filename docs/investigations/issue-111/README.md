# Issue 111 Computer Use focus-handoff qualification stop

This directory records the exactly one authorized Codex Computer Use-assisted, zero-operation
qualification of issue #110's durable operator-ready latch. It changes no controller, product,
fixture, workload, sampler, timeout, or downstream issue #104 authority.

## Qualification boundary

The task began from integrated commit `939aeaceac230941a720d61b3e5ab4ec16ca64e1` on the narrow
`codex/issue-111-computer-use-focus-retry` branch. Before repository access, one read-only Finder
inspection proved that Computer Use was callable in the available desktop session without a lock
or authentication boundary. No pixels were published.

The issue #110 readiness suite and all six issue #90/#91/#94/#96/#97/#98 predecessor suites
passed. The packaged dispatch suite passed 14 tests. The observer-enabled unsigned package build
transformed 338 frontend modules. Root `corepack pnpm check` passed formatting, lint, type checks,
74 test files / 578 tests with one intentional skip, the isolated semantic-retention proof, 24
Rust unit tests, and 28 native recovery tests. `corepack pnpm test:cross-platform` passed six PNG
checks and verified all eight registered deterministic fixture sets.

A fresh package rebuild under the task's newer local Rust toolchain did not reproduce the
canonical executable hash and was excluded from target use. The preserved exact issue #110
package was instead directly validated at canonical SHA-256
`5cf14de9836e9da96655572b35bb0e194d8ebc30360b6d6516f88825dcf7a15d`. Its product sources and
the preserved controller sources matched the integrated base. The exact canonical controller,
readiness observer, cancellation observer, sampler, and retention utility also directly reproduced
their recorded identities:

| Authority                              | SHA-256                                                            |
| -------------------------------------- | ------------------------------------------------------------------ |
| Durable-latch readiness controller     | `2e941986cf8d3a38e25e5319d84da93daa1572c95ce329ba512cea5a3c23fb51` |
| Canonical protected readiness observer | `9662c1664d44e93f58dc690a0fb78f08eb1f4751d84fc5790256866e768811ce` |
| Issue #104 cancellation observer       | `dee96628ba9b9cec31a49b0bd627466a636921f1f73535f1aac421834fbc0e6d` |
| RSS sampler                            | `1da835b06e7b2ffbe588e99fee2692d7cfa25f9e8117641c74a5abe73acc3dfe` |
| Retention utility                      | `c3b2d618b4dbddf9568d4d39334be9a3d0b074ae0b7a5464b4b7e30c5686441b` |

The worktree was clean and committed before target use. No candidate process or marker existed.
All prior evidence remained byte-unchanged.

## Exactly one controller preflight

The unchanged controller launched the exact candidate once with one declared focus action. It
validated the candidate and console session, proved the complete zero-operation boundary,
atomically published the owner-only issue #110 marker, and emitted `awaiting-operator-focus`. The
coordinator directly validated the exact marker token, bundle and executable identity, candidate
and console-session predicates, 120,000-ms handoff, one declared action, owner-only mode, and
complete zero-operation receipt.

Only then did the coordinator request one read-only Computer Use inspection of the packaged app.
That inspection did not return within the controller's 120,000-ms handoff. Therefore the authorized
focus click was never performed. The controller completed the full bound and made 1,702 focus
observations without detecting the exact candidate as Accessibility or Workspace frontmost. It
stopped before frame readiness, raise, retained-state validation, or the independent observer.

The controller returned **INVALID — PRE-DISPATCH STOP**, cleaned the marker, and terminated its
candidate. The delayed Computer Use inspection returned only after that terminal state. Because
Computer Use state inspection transparently launches an absent app, a separate terminal process
check then detected one stray zero-operation candidate. The coordinator sent one termination
signal and verified zero remaining candidate processes. This delayed tool-caused relaunch is a
strict candidate-launch-count violation and is not focus, product, or target evidence.

## Evidence and preservation

The complete sanitized receipt is in
[`qualification-2026-08-24/raw-results.json`](qualification-2026-08-24/raw-results.json). It
records Computer Use availability, the inspection disposition, zero clicks, controller states,
marker cleanup, the delayed relaunch cleanup, exact public identities, and strict zero-operation
proof. It contains no screenshot pixels, marker path or token, local private path, process
identifier, service identifier, username, or raw artifact location.

No credential, login, product control, fixture, sampler, artifact, destination, dispatch,
measurement, issue #95 operation, or issue #104 activity occurred. There was no retry. Issue #109
and #110 remain immutable, and issue #104 remains blocked and **UNCONSUMED**. A further readiness
attempt requires new authority; this result does not qualify issue #111 or authorize #104.
