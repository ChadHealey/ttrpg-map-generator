# Issue 179 — verification and review scope

The combined investigation suite through issue 179 passes **218 tests in 35 files**. The new
family tests cover all six complete paid owner sets, exact inherited quotas/classification,
root-and-far mode, finite candidate/vertex/member/site budgets, actual coast edges, bounded anatomy
and deterministic layout/subordinate diversity. R2 adds fifteen actual body-quota/corner checks,
unchanged pre-fit A role geometry, and equality of all 20 non-A owners across the six inputs.

The first comparison passes numeric gates in all six rows and verifies **38 captured sources,
68 relative runtime imports and 12 PNGs** against current files. Its independent
[visual review](visual-review.md) retains specific failures; numeric eligibility does not imply
family selection. The main task separately inspected all six native and six half images.

The second local gate passes all six complete owner sets, ordinary layout inventory and necessary
packing checks. Its verifier confirms **42 captured sources and 81 relative runtime imports**,
including the separate runner/revision record. Every accepted certificate explicitly records the
root-and-far mode. The first revision's full source/evidence remains unchanged.

The second full comparison also passes all six numeric rows and verifies **42 sources, 81 runtime
imports and 12 PNGs** against current files. Root and the independent reviewer each inspected every
native and half image. The [second review](visual-review-r2.md) retains the balanced-control R3
failure, so no family recommendation follows. All thirteen retained local stages replay successfully.

The runner captures transitive static/literal-dynamic sources before evaluation, repeats every
complete world result locally and checks source integrity afterward. The independent verifier
checks closure, exact input and receipt consistency, coverage arithmetic, PNG bytes/dimensions and
current-source equality. It does not claim independent scalar rerendering or cross-platform proof.
Preview/full total water and preview-owner tolerances remain unchanged; full owner values are
diagnostic. Sampled seam/pole and shared-anchor identities do not establish continuous topology,
feature survival, ocean connectivity semantics, non-neutral polar behavior or production extraction.

All retained local stages replay read-only from their own captured source. The pre-C3 stages retain
their actual old C source; `c-third`/`final-c3` capture the final first-revision runtime. R2's local
stages explicitly bind virtual module paths to their captured new source. The source overrides do
not edit live r1 modules. See [local findings](local-findings.md) for each stage and exact limits.

```sh
corepack pnpm exec vitest run docs/investigations/issue-169 docs/investigations/issue-170 docs/investigations/issue-172 docs/investigations/issue-174 docs/investigations/issue-176 docs/investigations/issue-177 docs/investigations/issue-178 docs/investigations/issue-179
node docs/investigations/issue-179/verify.mjs docs/investigations/issue-179/local-gate-r1 --require-current
node docs/investigations/issue-179/verify.mjs docs/investigations/issue-179/comparison-r1 --require-current
node docs/investigations/issue-179/verify.mjs docs/investigations/issue-179/local-gate-r2 --require-current
node docs/investigations/issue-179/verify.mjs docs/investigations/issue-179/comparison-r2 --require-current
corepack pnpm exec prettier --check docs/investigations/issue-179
corepack pnpm exec eslint docs/investigations/issue-179
```

Formatting, lint, relative links, public-content and staged whitespace checks accompany local
Conventional Commits and repository precommit checks. The previous broad root timeout and
successful focused/native recovery remain accurately recorded in
[issue-168 verification](../issue-168/verification.md). Unchanged production/native stages are
reused here; this is not represented as one clean root `pnpm check` exit. No production,
configuration, dependency, accepted-world or historical runtime source changed. No push.
