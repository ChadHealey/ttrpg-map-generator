# Independent literal review: whole-primary gulf prototype

**READY for the bounded first experiment, subject to its source and evidence freeze.** I found no analytic contradiction or missing role-partition responsibility in the proposed first-state table. This is permission to test the declared hypothesis, not a numerical certificate or a visual acceptance. I read the literal design and the frozen 188/172 partition and sampling implementations; I did not import or execute the new sampler, builder, certificate, measurements or renderer.

The 23 periodic anchors with three samples per span produce the declared 69-point coast. Anchor-to-sample mapping and all role intervals agree with the actual helper: L1 is 33–48, L2 is 12–24, and P wraps 60–3. Their occupied intervals are disjoint. Each far endpoint lies strictly within its corresponding role interval. The bay's 0–9 arc has exactly three edges on P and six on B. Its internal crossing at sample 3 is the P root endpoint, which whole-body mode permits when the shared edges are actual exterior coast. Neither mouth shoulder is a root endpoint. The P far endpoint at sample 0 is not prohibited by that root-junction restriction. The reverse pocket ring has the required closing directed mouth edge from sample 0 to sample 9; normalization must keep ring and mouth together.

The table fixes the full displacement rule before sampling. The peninsula's coast and its contributing neighboring anchors are unchanged throughout the two-dimensional box. The upper mouth's first following sample nevertheless depends on displaced anchor 5 through the cubic's second control; the design correctly includes that u term. The stated strictly negative radial-face shoulder signs remain negative over u in [-1,1], including every smaller amplitude. They establish the two incident-edge exclusions only. Continuous wedge exclusion for every other edge, pocket simplicity, role contact identity, cap containment and the witness remain actual certificate obligations.

The peninsula feasibility argument is internally consistent. Its lower collar span's four cubic controls lie at or below y=-.195, while the opposing upper span's controls lie at or above y=-.080. Convex combinations preserve that .115 planar separation, attained at the declared far endpoints. The original root line has normal (.4,.04); its distance at the fixed disk center is .0372/sqrt(.1616), about .09254. At the tip it is .1288/sqrt(.1616), about .3204. The largest stated control-line numerator is .129352, giving an upper below .322; the relevant projections remain on the original root segment. Thus the proposed extent bound concerns the original root, while the far cut supplies only a collar-width upper witness. It does not replace the root or mistake the minimum chain distance for a valid upper witness. These raw bounds do not certify the scaled spherical disk, extent, collar topology or all target margins.

The central anchor-chord area ledger is explicitly distinct from the sampled polygon and from the corner cases. The quoted shares are plausibly above the unchanged primary floors, but neither this review nor that ledger promotes them to sampled certificates. Both lobes, the peninsula, their fixed roots/far cuts/first disks, the body's interior witness and the bay remain mandatory on all cases. A failed primary cannot be relabelled subordinate. Regional changes may affect the body's core area, fitted scale and cap even when the gulf anchors stay fixed.

The single scale s=sqrt(4*pi*q/A), with A the disjoint surviving B-plus-role area, is the correct paid-body fit. The bay is already absent from the surviving coast and must not be subtracted again. All geometric coordinates and witnesses must receive the same scale; fixed role identity and non-coordinate metadata remain unchanged. These are body quotas with the declared detached fractions already accounted for. The prototype has no detached islands, placement, world coverage or public-control acceptance claim.

The corpus is exactly three quotas, five anatomies and four amplitudes: 60 outcomes, including coincident center cases. The three central quota panels at native and half size are the entire declared image inventory. Pin literal source, corpus, design/review, dependencies and tool versions before any useful geometry construction; retain every failure. The first result must precede a separately authored and independently reviewed second state, with no third state or hidden repair calls. A local visual pass would only support a subsequent family investigation. In particular, this new role-sharing relation may still look like opposing jaws or an assembled projection despite satisfying all numeric tests.

## Separate budgeted harness checkpoint

I also reviewed [harness-plan.md](harness-plan.md). Its explicit separation resolves the historical harness's extra-evaluation risk: each state spends at most 60 initial, 60 repeat and 60 replay body-entry calls; two states total at most 360. Verification, predecessor checks, final tests and independent review must spend zero such calls. The first runner must support the two-state schema without later changing captured shared source. A useful call increments its observed counter immediately before invocation, while an exclusive reservation consumes its phase even on interruption; those are distinct counts.

The post-replay authority file is an explicit independently reviewed external trust anchor, not evidence self-authentication. It can be pinned only after the one computational replay has completed. The pre-replay source/inventory check must therefore remain distinct from final authority-backed verification; it cannot require that future authority or claim final verified success prematurely. Completed replay is mandatory before a second state, and predecessor verification must never recompute the first geometry. With those stated distinctions preserved in code, the proposed inventory and one-shot claims have no evident budget or provenance contradiction. Actual proof-to-code review is still required before useful execution.
