# Issue 179 r2 independent runner and evidence review

No actionable correctness, evidence-contract or regression finding remains in the reviewed
[r2 runner](run-r2.mjs), [revision record](revision-r2.md), [constructor](templates-r2.mjs) and
[A coast](layout-r2-a.mjs). The [independent visual review](visual-review-r2.md) gives four
ordinary provisional passes but withholds the family recommendation because the balanced
control retains an R3 failure. Human decisions remain pending.

The new runner explicitly passes the r2 constructor to the frozen issue-172 gate and renderer.
Its changes from r1 are the constructor import, revision identifier and capture roots for itself
and the revision record. It retains the r1 runner in its source closure, creates a new directory,
captures before execution, checks current source bytes afterward and preserves exact per-row
repeats. No existing r1 source or output is overwritten by this wrapper.

The constructor changes its A import and revision metadata while preserving all r1 seed scopes,
quota formulas, B/C and subordinate construction, certificate options, finite selection budgets,
placement and field machinery. The A source changes five declared bay anchors and its fixed
prefit witness; its lobe and peninsula role declarations are unchanged. Uniform refitting can
change A's guard and therefore complete placement, so screen positions are not assumed stable.

Before the matrix, independently matched all ten artifact hashes in the
[A r2 local stage](local-diagnostics/a-r2-initial/manifest.json), replayed all fifteen certificate
receipts and confirmed that its captured source equals the final `layout-r2-a.mjs` bytes. The
local manifest's `layout-a.mjs` entry is a logical capture name; its saved source text establishes
the revision rather than the current r1 file at that name. The focused constructor tests cover
all fifteen quota/anatomy cases, unchanged prefit role polygons, all six paid owner sets and
exact equality of all twenty non-A owners. Their assertions were reviewed; the main task ran
the three tests successfully.

Independently ran the read-only [verifier](verify.mjs) with `--require-current` against both
[local-gate-r2](local-gate-r2/manifest.json) and [comparison-r2](comparison-r2/manifest.json).
Both pass for all six exact retained inputs, **42 captured sources and 81 relative runtime
imports**, with no current-source differences. The comparison additionally passes all twelve
PNG hash/dimension checks, individual/aggregate row consistency, arithmetic and numeric-stage
checks. All six rows report numeric gates passed. Accepted certificates retain the explicit
`root-and-far` mode; the separate r2 runner and revision record are present in the captured
closure. Preview-owner error remains the adopted gate; full-owner error remains diagnostic.

All six native 1600×800 and six half-size 800×400 images were independently inspected. This
review's remaining visual finding comes from those exact world images, not the local panel,
layout inventory labels or numeric success.

Hash and receipt verification establishes captured source closure and artifact consistency. It
does not independently rerun the scalar grids or all constructor repeat executions, establish
cross-platform equality, prove post-extraction feature survival or supply human acceptance.
Production remains unselected. Both r1 and r2 evidence and their separate visual conclusions
must remain available.
