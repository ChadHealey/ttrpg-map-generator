# Independent D3 implementation checkpoint

**The reviewed private harness is ready for final source freeze and the main task's separate manifest clearance.** No actionable correctness or boundary finding remains. This review ran only public/injected preliminary tests and read-only source inspection; it did not execute the actual constructor, placement, field or semantic matrix. The [design checkpoint](independent-design-review.md) remains the authority for the fixed scientific scope.

## Fixed source, inputs and work bounds

The machine corpus contains exactly the published 14 paired and 16 boundary inputs. Public exact-key parsing precedes the two count-1 capacity exclusions, which perform no construction. The fixed constructor is issue184 material r3 from world comparison-r2; both retained manifest/results and every transitive frozen runtime module are checked against that checkpoint before matrix imports. The public core/generation closure resolves only their declared package entries, with exact package metadata, trusted paths and current source equality before compilation. Node, TypeScript, Prettier, package/lock and source authority belong to the capture.

The budget accounts for attempted constructor, placement, unique-field, scalar, partition and semantic calls. The frozen per-owner 16-candidate limit and construction audit bound candidate work; each scalar call is charged before evaluation. There is no additional in-pass constructor repeat. One pass and one replay are the declared limits; analytic rows remain no-attempt rows in both. The actual matrix has not been run by these preliminary tests.

Deduplication retains the entire construction and placement receipts, removing only physicalKmPerRadian, fragmentationBand and oceanConnectivity at construction.recipe. It does not remove any owner coordinate, island, quota, certificate, selected candidate, site ledger or frame. The outer input remains separately retained per row. Exact serialized equality, not a hash alone, proves sharing, and an expected mismatch or more than 18 unique fields prevents scalar execution. Failed empty owners are not supported duplicates. Requested ocean outcomes remain independent even on one shared reader.

## Public sampling and measurement

The bridge uses the actual full-profile public contour constructor at lower tick zero, quantizer and classifier. Each canonical anchor invokes only the frozen continuous field evaluate method, normalizes its finite value and quantizes once. The private million-tick raw method is unused. The full profile has exactly 2,095,106 unique anchors, with one sample per pole. Public storage addressing and planet angles supply the loop; canonical scalar/tick hashes use explicit little-endian bytes.

The independently computed tick>0 bits are checked against every public H classification sample before creating the immutable public reader. Coverage is the actual public rounded classifier result with the unchanged 25-basis-point gate. Full owner errors are diagnostics. A partition failure retains already completed sampling/classification evidence. Partial sampling records its completed prefix, expected count, zero-padded bit-buffer scope and stage error rather than pretending to be a complete profile.

Public four-neighbor sphere partitioning and public integer row weights drive the marine and polar analysis. The latitude cutoff is exactly 715827883 ticks, pole weights are zero, and BigInt cross-products determine paired cap ordering without rounded percentages. Land's z-squared moment and the primary-body LAEA hull deficit remain floating diagnostics. Detached islands are excluded from that hull metric. Original body/island anchor counts and sampled memberships are retained, so missing components or mergers need not be mistaken for intentional fragmentation. They do not supply D1's missing continuous-to-extracted role proof.

The semantic helper directly calls public segmentAtlasWaterBodies on the unchanged reader. Its graph check includes reciprocal valid links, one basin root per open component, the greatest open region being the basin for single/majority, the 90% majority threshold, and all open regions being separate rooted basins for multiple. The corrected connectedMajority predicate allows a disconnected 90/10 one-root-per-component graph and rejects 89/11; it does not impose a global single-root rule. One allocated open region preserves zero-versus-one core ambiguity. Returned analysis indices remain transient; no accepted record or semantic EntityId is fabricated.

## Review correction and failure preservation

Initial pair assembly could emit a positive polar direction result after failed coverage or a later field-stage failure, because it tested only the presence of polar measurements. That reporting defect is fixed before capture. Raw arithmetic remains visible, but positive direction eligibility requires all three source geometries valid, fields complete and coverage gates passed. A complete control diagnostic additionally requires the semantic outcomes. Fragmentation likewise distinguishes available source concavity from complete control eligibility. The regression covers both failed coverage and a post-sampling partition failure.

Independent row failures are retained through construction, placement, sampling and semantic stages. A shared sample failure marks each dependent semantic request not attempted; one semantic exception cannot hide a counterpart or cause resampling. Artifact I/O errors abort rather than overwriting prior evidence. Full public-domain support and selected proposal remain explicitly false/null regardless of narrower measured successes.

## Evidence and independent checks

Preparation writes one exclusive source manifest and compressed exact closure before execution. Run and replay require trusted current source/checkpoint equality first. Output names derive only from the fixed 30 IDs and validated hexadecimal field keys; exact inventories and hashes reject missing, extra or escaping paths. Replay recomputes exact receipts and hashes. Hashes-only verification remains a separate integrity check and does not claim computational reproduction of coherently altered scientific results.

I independently ran:

- `pnpm exec vitest run docs/investigations/issue-189/harness.test.mjs docs/investigations/issue-189/evidence.test.mjs`: **15 tests passed in two files**.
- `pnpm exec tsc -p docs/investigations/issue-189/tsconfig.json --noEmit`: passed.

Those tests cover public parsing/profile rejection, analytic no-attempt behavior, budgets, corrected marine graph rules, exact polar ordering, body-only concavity, strict dedup projection, failure retention, canonical byte order, frozen source authority, hostile coherent source/input replacement, artifact shape and missing hashes. They use injected stage outputs or previously retained geometry; they do not launch the declared matrix.

Before final manifest preparation, both independent reviews must be included in the fixed source inventory and all final captured text must be formatter-idempotent. That final authority check, the main task's execution clearance, actual retained-data review and truthful result disposition remain separate. No production adaptation, geometry repair, target change, preview or render is authorized by this source checkpoint.
