# Independent first-state source and budget checkpoint

**READY for final formatting, prospective-manifest review and the main task's separate execution clearance.** No actionable source or budget blocker remains. I inspected the literal constructor, fit/evaluator, pure schema/corpus, renderer, source collector/loader, phase runner and verifier. I ran only the six pure preliminary tests. No new sampler, useful constructor, body certificate, renderer or computational replay was invoked by this review.

## Literal and certificate wiring

The first state's 23 anchors, U/V coefficient entries, four amplitudes, tension .12 and three samples per span match the [reviewed literal](independent-literal-review.md). The role ordering and sampled indices, fixed root/far incidences, displaced lobe disk centers, P disk, body witness and bay witness also match. The constructor rejects a nonpositive mother coast before partitioning. It uses the frozen 188 partition entry, so the P-bank overlap is actual exterior coast and the pocket's ordered mouth follows the reviewed reversed-ring construction. No branch searches for a more favorable root, far cut, disk or bay label.

The fit computes the disjoint surviving B-plus-role area and scales once by sqrt(4*pi*q/A). It never subtracts the bay a second time. The candidate forces its primary flag and empty detached inventory after spreading the partition, stitches the body from the actual roles, and uniformly scales all coordinate arrays and witnesses. Its current partition has no unrelated numeric-pair metadata that would be incorrectly scaled by that mapping. This is explicitly a body-only quota experiment, not a complete detached owner.

Every candidate calls the unchanged 188 whole-body certificate with nominal clearance .05 and root-and-far collar-width mode. The whole-body helper supplies wedge-geodesic bay semantics; the constructor does not suppress a failed primary criterion. A thrown constructor/certificate is retained with the attempted-call flag, and the other declared cases continue. All 60 ordered inputs use exactly the three quota expressions, five anatomies and four variations. Coincident center variants remain separate declared outcomes.

The renderer takes only the three central, variation-zero quota cases. Its fixed 900×320 mask spans 2.5 chart units in each panel, and 450×160 half pixels copy the native even-coordinate mask. It renders the actual sampled body boundary; there is no separate smoothing or geometry repair. Missing constructed bodies remain blank with an explicit panel ledger. Failed certificates do not become visually accepted merely because their geometry can be displayed.

## Bounded execution and authority

The record path contains exactly two evaluator calls: one initial 60-case pass and one repeat. Replay contains exactly one 60-case evaluator call. Each evaluator increments its constructor counter immediately before the fixed case, and its certificate counter before the single private certificate entry. Internal legacy delegation is part of that one body entry. Thus each state reserves at most 180 slots and two states reserve at most 360. There are no geometry calls in inspect, final verification, predecessor verification or authority drafting.

Record creates its state directory exclusively; repeat and replay claims use exclusive writes before loading useful runtime. A partial or failed claim cannot silently restart. A repeat/replay mismatch preserves its compressed result and false equality receipt and then stops; it cannot overwrite the initial images. Reserved slots and observed successful phase counts are distinct, and a crash cannot donate the unused reservation to another candidate. The final verifier requires complete matching phases; it does not convert an incomplete capture into verified success.

The source collector starts from the selected literal, evaluator and corpus and recursively captures their static relative investigation imports. It rejects dynamic runtime imports and import.meta in that graph; the separate loader rewrites the captured static graph to in-memory module URLs. Fixed runner/verifier/encoding sources, package/lock metadata, tool versions and design/reviews are also bound. Computation receives a freshly captured current closure, and replay first compares the stored source bytes and manifest against that trusted current closure. A self-consistent retained manifest alone is insufficient. Post-phase current-source checks reject drift.

The reviewed plan now accurately states that first-state approval is the main task's external approval of the exact prospective manifest; phase claims bind that manifest hash. State 2 additionally captures its explicit authorization, complete new literal/design/review, prior disposition and first-state receipt/authority hashes. It verifies the completed predecessor through the zero-call verifier. Shared first-state runtime and its selected closure must stay unchanged when the separate second literal is introduced.

Final authority is the separately reviewed `authority/state-1.json` or `authority/state-2.json`, created after the authorized replay and outside the evidence directory. It pins the exact manifest and every artifact digest. Pre-replay inspection deliberately does not require this future authority. Final verification does require it and reports zero geometry calls. That external review/pin is the stated trust anchor against coherent replacement of all self-hashed results; hashes alone do not prove geometry. Root must independently compare the actual replay receipt digests before accepting that authority file.

## Preliminary verification and remaining checkpoint

I independently ran `pnpm exec vitest run docs/investigations/issue-190/pure.test.mjs`: **six tests passed in one file**. The tests use pure corpus/schema checks, synthetic error receipts, fixed prerequisite text and AST inspection of execution paths. They do not import the useful literal or evaluate geometry. They reject malformed anatomy/variation/state values, altered result counts/order and changed second-state prerequisite hashes, and check the two/one/zero call-path separation. Source inspection supplies the broader proof-to-code check; the AST assertions alone are not a general static proof of program behavior.

Before capture, finish formatter-idempotence checks across the actual selected source inventory, including this review, and obtain the main task's exact manifest clearance. After capture, the sole designated replay supplies computational reproduction. Subsequent independent review and tests must use only retained data, zero-call verification and PNG decoding. Actual numeric outcomes and the native/half visual disposition remain unknown; this source checkpoint neither certifies these shapes nor permits an unreviewed second state.
