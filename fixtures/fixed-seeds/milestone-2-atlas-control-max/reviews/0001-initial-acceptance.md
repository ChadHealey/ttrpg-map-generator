# Milestone 2 control-maximum row — initial generator acceptance

## Intended behavior

Accept version-1 generator evidence for every declared numeric maximum, one-dominant
distribution, multiple-basin intent, and ocean-biased poles. The full partition must remain valid
and report semantic connectivity as unverified.

## Changed evidence

- `fixed-seeds/milestone-2-atlas-control-max/expected/baseline/atlas-land-water.kernel.canonical`

## Version and compatibility consequence

This accepts atlas field, classification, generator-manifest, parameter-schema, preview,
realization, sampling-policy, seed-derivation, and deterministic-stream version 1 at the maximum
control boundary. It does not establish persistence or semantic-ocean compatibility.

## Evidence reviewed

The full-profile primitive traversal hashes, exact maximum controls, identities, versions,
realization, transient proxy observations, stable diagnostics, preview nesting, seam, poles, and
progress were reviewed. The classification intent is excluded from the macro parameter record.
Semantic basins/entities, coastline geometry, SVG, visual, and authoritative package evidence are
not applicable to issue #58.
