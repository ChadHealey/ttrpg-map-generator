# Issue 186 independent retained-result review

**No actionable evidence or result-reporting defect remains. Neither Z nor H establishes the required production realization.** The retained failures support that disposition without weakening the original geometric targets or claiming that every alternative realization is impossible.

This review follows the separate [implementation review](independent-implementation-review.md). It reads the completed evidence and final findings; it does not evaluate the six fields again. The author completed the one authorized computational replay, as recorded in [verification](verification.md). I independently ran `node docs/investigations/issue-186/run.mjs --hashes-only`: verified=true, replay=false, six rows, two profiles, 24 attempted policy evaluations, eight synthetics and 116 sources. Current trusted source/checkpoint equality and the complete artifact inventory pass.

After this review’s initial hash check, the precommit formatting correction changed only the current policy document. The separate [post-capture formatting review](postcapture-format-review.md) verifies the pinned historical evidence under that exact transition. The original runner intentionally rejects current document drift; no further computational replay occurred.

## Independent retained-data checks

I compared all 12 profile receipts with their corresponding [decision](evidence-r1/decision.json) entries, including coverage, shared-anchor results, extraction status, failures and ineligibility. For all 23 completed extractions, I decompressed the saved ring archive, recomputed its semantic JSON SHA-256 and compared it with the receipt. Raw/simplified ring counts, provenance counts and distinct sampled-component keys agree. Every retained simplification displacement is valid and has at most one consecutive removed vertex. These are read-only artifact checks, distinct from replaying field sampling or extraction.

All six preview/full pairs report exact normalized-value and tick agreement at their 130,562 shared anchors. The profile sizes are the declared 130,562 and 2,095,106 unique anchors. No sample has ambiguous owner attribution or clamp saturation. All 12 total-water errors pass; the largest is 2.1598 basis points. All preview per-owner errors pass their unchanged `0.25 / ownerCount` percentage-point limit; the largest observed error is 0.012123046511625546 percentage points. Full per-owner values remain diagnostics.

The admitted Z/H pairs have identical sampled component counts and corresponding raw ring component/owner associations, although their contour coordinates and displacement measurements differ. This is agreement of the retained sampled graph and provenance, not equality of both contour geometries.

## Concrete preservation failures

Every preview row contains at least one sampled component whose boundary provenance spans multiple original paid components. At full resolution, normal-04 still merges `owner-0/body` with `owner-0/island-0`; fragmented-islands retains six merged sampled groups. Multiple rings around one sampled component are not counted as separate mergers. The final [findings](findings.md) identify the affected IDs and ring indices consistently with the receipts.

Fragmented preview additionally has no land anchors for `owner-3/archipelago-5`, `owner-3/archipelago-6` and `owner-4/archipelago-6`. These are actual failures to preserve those paid pieces in the sampled land classification. They do not imply that the original certified polygons overlap, or that separate owner guards merge.

Connected-majority full has two zero ticks. Z rejects this profile under its predeclared restricted-domain rule; H independently extracts it. The retained status is a refusal, not an exception, skipped anchor, silent threshold change or fallback. Across the entire experiment, 24 policies are attempted and 23 extractions complete.

The largest measured raw-vertex to actual simplified predecessor-chord angular upper estimate is 0.001080586486176868 radians. Its small value does not prove original-field survival or the required two-way correspondence. The separate cell/quantization error ledger and conditional role margins remain correctly limited: preview's generic bound exceeds D=0.01, and even full's smaller one-sided bound cannot establish all retained feature margins. Witness diagnostics are finite observations, not reconstructed root, collar, area or extent certificates.

## Disposition

The investigation establishes fixed counterexamples to paid-component preservation and leaves the extracted-role transfer proof open. Its negative result is useful and reproducible. A successor needs a reviewed survival/separation and correspondence contract before policy or version selection; changing the geometry, sampling budgets or adopted targets is not authorized by these receipts. No production, visual-family or human acceptance follows from the successful coverage and public topology checks.
