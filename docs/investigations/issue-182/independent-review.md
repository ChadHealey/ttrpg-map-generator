# Issue 182 independent recovery review

No actionable correctness, regression or provenance finding remains in the reviewed
[large recipe](layout-large.mjs), [extended constructor](templates.mjs),
[recovery runner](run.mjs), [baseline comparison](compare-baseline.mjs),
[guarded local verifier](verify-local.mjs) and [recorded conclusions](README.md).
The bounded recovery succeeds numerically; this is not whole-world visual acceptance.

## Geometry and compatibility

The new coast is sampled from fixed anchors, partitioned at declared role/root/far indices
and translated by a fixed chart offset before quota fitting. The shorter broad taper and
smaller oblique bay are changes to authored geometry, not to the certificate or adopted
targets. The valid far crosscut supplies the tighter width upper witness; extent continues
to measure from the original root. The complete body and detached categories remain paid
from the exact owner quota. Six real coast-edge sites, four offsets and the finite member
search preserve the existing island budget and final component checks.

Candidate indices 0–11 delegate directly to frozen issue-179, preserving its validation,
geometry, errors and streams. The owner loop retains its original quota/primary rules and
selection priority. Indices 12–15 append one primary-only coast with four explicit island-site
variants. These are not four independently authored coast recipes. Invalid or exhausted
requests fail explicitly at the sixteen-candidate boundary. The separate extension revision
declares the changed private behavior while preserving the old construction fields for exact
compatibility comparison; this is not a production provenance contract.

Independently ran all three focused test files: **12 tests pass**. They cover the actual
large paid-body quota at five anatomy cases; fixed roots and exposed island sites; malformed
inputs and input immutability; exact old-twelve delegation and six retained constructions;
recovery only after retained failures; category area/count payments including independent
zero abundances; all four fallback variants; explicit sixteen-attempt unsupported-cap failure;
and the local-source trust guard.

## Immutable recovery evidence

Independently executed:

```sh
node docs/investigations/issue-182/run.mjs --verify
node docs/investigations/issue-182/compare-baseline.mjs
node docs/investigations/issue-182/verify-local.mjs
node docs/investigations/issue-182/render-local.mjs --verify
```

The recovery verifier checks the exact 128 additional-default probes from issue-180, with
22 captured source records and 17 frozen issue-179 runtime matches. Trusted current closure,
exact source/hash inventories, fixed dependencies and frozen authority are checked before
saved-source execution. All artifact hashes and inventories match. All 256 repeated calls
reproduce their complete persisted rows and summary; no scalar grid is evaluated in this lane.

The separate baseline comparison confirms all 128 construction-and-placement passes:

- All 38 previous failures recover at candidate index 12; no later fallback is selected.
- All 90 prior successes preserve exact construction fields, receipts, owners and placement,
  apart from the explicitly excluded extension metadata.
- Every original candidate receipt and previously accepted owner also remains exact in the
  38 repaired cases. No old failure, subordinate or paid quota is omitted.
- Maximum selected guard is `1.2465383684777747` radians. Minimum final pair gap is
  `0.05041278447034725` radians, above the unchanged nominal gap and numeric slack.

These comparisons are tied to the two completion hashes recorded in
[verification](verification.md). Construction success is not substituted for placement;
both stages and their retained attempts are present.

## Local evidence and review correction

The inherited local helper initially trusted self-reported source hashes before executing
saved code. Independent review identified that gap. The new narrow `verify-local.mjs` entry
checks the known entry, fixed source/dependency keys, safe exact artifact inventory, source
text equality with the trusted current closure and dependency hashes before invoking replay.
Its tests reject a coherently rehashed source replacement before execution and reject a path
escape. The historical writer and captured evidence remain unchanged; use the guarded entry
for local verification.

Independently matched all eight recipe-1 saved sources and all ten artifact hashes, then
replayed its five certificate reports and exact PNG. The guarded command also passes.
The paid-owner renderer reproduces the exact 400×400 local panel from retained default-001.
Both that image and the recipe-1 center image were actually inspected.

The local silhouette has unequal broad lobes and a shorter broad-based taper without the
earlier narrow terminal head. The pointed lower tip and compact angled right-hand recess
remain visual concerns. Islands sit at visible margins with unequal sizes. These local
observations do not establish spherical arrangement, half-atlas readability, absence of
repetition across seeds or resolution of issue-179's R3 finding.

The result supports completion of this bounded quota-recovery hypothesis. It supplies no
control-domain repair, scalar coverage proof, production extraction/feature-survival proof,
cross-platform equality, human selection or production adoption. The count-one/45%-water
cap obstruction remains an explicit failure. Any world comparison needs its own bounded
issue, preserving both the recovery evidence and earlier visual rejections.
