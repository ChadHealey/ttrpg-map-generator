# Third-state geometry review

The final local capture implements the approved third state exactly. I found no actionable
implementation or certificate-replay defect. Its local image is credible enough to test in the
final paid readiness gate and, if that passes, the final fixed world matrix. It does not establish
a recommended family. I authored this geometry hypothesis; this is independent implementation
verification and a separate assistant image judgment, not independent authorship or human acceptance.

## Fidelity and preservation

Compared with recipe 2, A changes only its two bank control arrays and declared bay witness to
the approved constants. Large changes only its two bank arrays. Both mouth pairs remain exact.
The B layout and splice helper are byte-identical to recipe 2. The splice intervals, finite
sampling and fixed Large chart translation remain unchanged.

I separately evaluated the two retained recipes across all five anatomy cases and four variations
for each layout: 60 paired raw cases. Attachments, roots, far cuts, collar disks, interior
witnesses, mouth coordinates and six island anchor edges match exactly. Every declared site is
an actual directed exposed edge of the stitched body. The complete B result is unchanged in
every pair. A's witness is exactly (.350,-.070); Large's translated witness remains unchanged.
These are raw-coordinate identities: changed body area legitimately changes the later uniform
fit, paid geometry and potentially placement.

The exterior cubic joins align tangent directions (G1), with the existing .35 handle factor;
their parametric derivatives do not have equal magnitudes. Only the two bank derivatives at
each inner head match exactly. The authoritative coast remains the sampled polygon, not a
separately smoothed rendering or an analytically credited spline area.

## Local evidence

The guarded replay command passed against the pinned current recipe closure:

```sh
node docs/investigations/issue-184/local-evidence.mjs --verify recipe-3
pnpm exec vitest run docs/investigations/issue-184/splice.test.mjs
```

Replay reproduced all 140 receipts and the panel exactly. All 140 certificates and all four
focused tests pass. The retained minimum removed-body shares are .0246463072 for A,
.0292871205 for B and .0315504370 for Large, each above the unchanged .02 floor. Minimum
depth lower bounds are .1703388935, .1854073371 and .2308899564 respectively, each above .15.
These values come from the retained sampled-polygon receipts, not an assumed spline area.

## Image disposition and remaining gate

I viewed the exact [A/B/Large local panel](local-diagnostics/recipe-3/panel.png). A's lower bank
now rises into the inner head and its lower projecting margin reads shorter and more oblique
than the long state-2 shelf. Large's upper bank makes a visible shallow arch and turns toward
its shoulder, rather than spending the entire run on one inclined direction. These are material
macro changes. Both still have conspicuous opposing lips; the local view cannot establish how
those margins project across the required worlds.

B remains unchanged. The wider independent first-matrix concerns about B, C and repeated
control-row mouths have not been repaired by these A/Large changes. The main and independent
world reviewers previously differed on their severity; that disagreement remains relevant.
Every final world must be reassessed at native and half size after the paid construction and
placement gate. Numeric success cannot substitute for that judgment.

Three material states are now spent. No corrective fourth state is implied by this local
disposition. A surviving complete-family failure in the remaining matrix ends this bounded
experiment without selecting the family, changing targets or claiming human acceptance.
