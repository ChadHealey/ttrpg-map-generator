# Independent source review before useful-fixture capture

**The private whole-body mode is ready for the one declared two-fixture capture, subject to the main task's final source-freeze clearance.** No actionable proof-to-code or harness defect remains in the reviewed implementation. Neither useful fixture has been imported or evaluated for this review. The [design checkpoint](independent-design-review.md) records the preceding analytic assessment; numerical and local visual outcomes remain unproved.

## Positive-body delegation and omitted responsibilities

The wrapper delegates missing/explicit interior mode directly to frozen 178. Whole-body mode first validates a finite, bounded wedge bay, then passes the unchanged candidate data and options to 178 with only bay set to null. It retains the original primary flag, quota, roots, far cuts, disks, islands and submitted body boundary. The filter matches all four fields of the intentional missing-bay diagnostic, requires exactly one for a primary and none for a subordinate, and preserves every other failure. An early invalid primary return cannot qualify by having no bay diagnostic.

This matches the actual frozen source's bay dependencies: ring/schema validation and vertex/radius collection, the old bay block, the island/E conflict and the primary missing-bay record. There is no additional bay-dependent positive-role path. New E vertices must match a complete arc of the certified S boundary before delegated cap metrics are used, so removing E from the delegated ring collection cannot reduce the accepted radius or unique-vertex maximum. The separate witness still receives the unchanged cap/side checks. Existing positive-union and all applicable primary/subordinate checks must succeed before whole-S containment is used.

## Exterior adjacency and protected water

Topology normalizes S and E privately while checking the supplied original directed mouth first. The nonmouth E sequence must be the opposite traversal of one complete, proper S arc, using exact coordinate identity and consecutive vertices. This excludes hidden/internal roots, skipped vertices and partial or disconnected coast matches. Mouth shoulders at any root endpoint reject. A root endpoint strictly inside the shared shore can remain, because its adjacent exposed S edges—not the internal root chord—supply that shore.

The contact scan derives allowed full edges and their necessary endpoints from this proved arc. It rejects other crossings, overlap, contact and positive-interior membership in both directions. Separate B/role scans derive their own allowed edges from the same S arc; roles receive no caller-selected waiver. Islands have no contact exception. The witness must be strictly in E and outside S and every positive component. Stitching E onto S must produce one simple U0 with the exact mouth-chord replacement and additive planar area within the inherited residual tolerance.

The new mouth helper changes the frozen wedge check's positive-polygon inventory to certified S and detached islands. All other orientation, side, cap, whole-segment clipping, opening/depth/ratio/removed-area and slack rules are retained. Exact positive-role containment justifies not treating internal B/role boundaries as coast. The existing outward-lens exclusion proof therefore still applies. Primary removed share uses the unchanged surviving body area; there is no double subtraction of E or unverified lens area credit. The explicit mode/provenance identifies this broader private contract without upgrading its binary64 diagnostic assurance.

## Fixed partition and useful literals

The partition wrapper reuses frozen role partitioning with bay omitted, then constructs E from the predeclared full-coast interval. It leaves the role/root/far/disk identities and surviving S unchanged. Bay overlap with role coast is permitted; no favorable role is found after fitting.

Source inspection confirms the two builders implement the reviewed peninsula and reflected-lobe literals, including each fixed root, far cut, first disk and witness. Both explicitly retain primary false and no islands; each quota is its own surviving B-plus-role planar area divided by 4π. The lobe reflection also reverses rings and ordered mouth together. None of these source observations is an executed certificate or visual pass.

## Harness and verification

The runtime closure is discovered statically from fixed entry paths. Imports are restricted to investigation-relative modules and Node builtins; dynamic imports and import.meta inside that closure reject. The manifest binds the full runtime, capture/runner sources, design, this review, harness plan, root package/lock and tool versions. Recording uses a new exclusive directory and writes source, snapshot and corpus bytes before the first useful builder import. Both cases retain certificate failures or construction errors and repeat exactly; no repair state or extra input exists.

Replay validates fixed artifact names, exact trusted current manifest and source text, corpus and artifact hashes before loading the matching in-memory closure. A coherently rehashed foreign snapshot cannot authorize code execution. It reconstructs both cases twice, compares report/summary/image bytes and checks source again. The panel is exactly the declared two-cell 640×320 local mask and its 320×160 even-coordinate half image, with equal chart scale on both axes. No world or primary-family inference is attached.

I independently ran `pnpm exec vitest run docs/investigations/issue-188/certificates.test.mjs`: **11 tests passed**. They use historical geometry/receipts and a combinatorial partition fixture, never either useful 188 builder. Coverage includes exact legacy delegation in both collar modes, ring/mouth reversal, malformed modes/geometry, actual coast matching, E/witness/island and role contacts, each removed primary attachment, bad far/disk/body data, early primary invalid input and a surviving peninsula-share failure. Review requested the focused primary regressions; they are now present and pass.

After authorized capture, separate uncaptured provenance tests will exercise source, inventory, image and coherently false-summary tampering against disposable evidence copies before final replay. They must not change captured sources or useful literals. A read-only source capture found 22 sources; independently formatting each with the repository configuration left its current bytes unchanged, and a second formatting pass was identical. This loaded only capture/parser helpers and executed zero useful fixtures. Later tests, actual local images and retained feasibility outcomes require their own final review; this source checkpoint authorizes no third fixture, tuning loop or production selection.
