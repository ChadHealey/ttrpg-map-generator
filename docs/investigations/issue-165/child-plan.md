# Proposed decision and conditional successor drafts — issue 165

**Proposed decision: no selection.** Both bounded realizations were human-rejected on every row.
Envelope quotas also violate the declared coverage tolerance on three rows. Do not adopt either
prototype as v3 or expand this rejected comparison into more tuning. This is an issue-owned
proposal; [ADR-0029](../../adr/0029-separated-macro-landmass-field.md) is not edited or accepted anew.

## Smallest next decision

Decide whether to fund a design-only feasibility step for **budget-aware construction before
contour calibration**, instead of another fixed-quota contour experiment. The evidence says a
quota cannot create missing capacity or internal hierarchy. Merely redistributing the failed
quotas would abandon this experiment's intended area ratios without proving better anatomy.

Proposed bounded discovery D2: specify one capacity-feasible placement/shape-budget policy using
only this retained evidence, normal-01's two insufficient caps, and the existing visual contract.
Deliver a mathematical feasibility condition, explicit allocation to broad interior/secondary
lobe/peninsula/embayment, maximum contour displacement relative to those features, fixed operation
budget and a no-proposal condition. Explain how the policy avoids allocating area at the guard.
Do not generate another full matrix, implement production code, add an algorithm family or introduce
a dependency in that design step. If no such bounded specification can be justified, report that
result and require an explicit product/architecture decision before further algorithm work.

D2 is a **proposed draft, not yet authorized or a new GitHub issue**. A later separately authored
comparison would still require the unchanged six inputs, all-row human review, analytic geometry,
continuous contour, exact repeats and declared coverage failure. It cannot lower the visual bar.
The current task stops after evidence, decision, verification and review handoff.

## Conditional C1–C3 revalidation

The [issue-164 implementation drafts](../issue-164/child-plan.md) retain their separate boundaries.
Their primary entry paths remain present in the prerequisite checkout, but their numeric design
assumptions cannot be finalized because no field was selected here. No successor GitHub issue was
created or modified. Re-author their exact contracts after a selected discovery; do not inherit
this prototype's contours, axis formulas or counter stream by accident.

| Draft                | Readiness | Retained scope and verification                                                                                                                                                                           | New blocker/disposition                                                                                                                                                           |
| -------------------- | --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| C1 compatibility     | NOT READY | Core/persistence matching v3 provenance, strict mixed-version rejection, v1/v2 byte-stable generator-free reopen; focused DTO and compatibility tests. Existing #162 support is already present.          | No selected v3 accepted record contract. Per-owner contours must not silently add a persistence field. Stop if layout redesign is needed.                                         |
| C2 generation        | NOT READY | Generation adapter only; exact selected v3 field, all nine controls, bounded failures, production anchors/continuity, 128-seed sweep, human production-entry evidence and macOS/Linux canonical equality. | Both quota realizations rejected. A future selected spec must demonstrate capacity and internal hierarchy; numerical area shares alone do not satisfy shape quality. Requires C1. |
| C3 adoption/evidence | NOT READY | Deliberate new-atlas dispatch, additive registered v3 cohort, existing v1/v2 reopen, full workflow/cancellation/export checks and human review.                                                           | Requires successful C1/C2 plus full production cohort (12 defaults/6 controls), not these diagnostic PNGs. No automatic upgrade or accepted-map replacement.                      |

C1 remains C3 complexity; C2 and C3 remain C4 pending fresh scoring of an actual selected design.
No runtime/model recommendation is reissued for hypothetical future work. Each would require a
new bounded issue with a current execution profile and dedicated read-only review before publication.

## Compatibility, streams, versions and M3

Retain issue-164's [compatibility proposal](../issue-164/proposed-adr.md) in full. Accepted v1 and
any user-accepted v2 data, IDs, constraints, locks, decoration and edits remain immutable; reopening
invokes no generator. Rejection of candidate v2 geography does not authorize replacing accepted v2
user data. Never reuse version 2 for changed canonical output.

An eventual output-changing selected macro field uses behavior/generator version 3 and an atlas
manifest version that truthfully identifies the composition. Preserve existing package/record,
sampling, quantization, classification, coastline, seed-derivation and context versions unless
their own contracts change. Strict matching provenance must be added independently of adoption.
Use existing production seed scopes with documented stable owner/subfeature mapping; this
investigation deliberately reused r2's private scopes to hold placement constant, not to define v3.

Local macOS repeats establish no Linux proof. Require cross-platform canonical field, partition,
semantic and reopened-output equality before production adoption; sin/cos/acos/hypot, floating area
sums, threshold ties and image compression remain risks. Keep old registered evidence byte-stable,
add new v3 rows only through targeted reviewed fixture updates after selection.

#161 remains the V3 tracking outcome. #148's refreshed inherited-context evidence follows accepted
V3 production geography; #150's visible-exit review follows that evidence. Existing parent snapshots
and descendants retain their accepted lineage; changed parents may mark context stale through the
existing policy, never regenerate children or reinterpret stored source versions. M3 overlays and
M4 coastline detail cannot provide missing M2 continental anatomy. No issue states are changed here.
