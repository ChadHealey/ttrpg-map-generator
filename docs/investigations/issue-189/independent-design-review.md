# Independent D3 design checkpoint

**The fixed control investigation is ready for bounded implementation and subsequent source review.** This clears the [revised policy](policy.md), not execution of its constructor/field matrix or a production adapter. No constructor, field, geometry or semantic measurement was executed for this design review.

The declared public controls match `parseAtlasControls`, their validator and the product control table. Public count remains an intent; policy 1's 20% land threshold cannot support more than five semantic continents. Paid owners are not semantic continents. The sole-owner capacity inequality at chart radius 1.4 includes all paid islands and excludes water 45 and 58 before construction. It does not prove the adopted targets impossible, nor does water 59 become feasible merely by escaping this inequality.

The selected source is precisely issue-184's r3 template captured in comparison-r2, distinct from D1's comparison-r1 checkpoint. The plan binds the actual entry and transitive source to that retained manifest. It preserves all construction, site, placement and certificate budgets. D1's scalar policy H is used only as a declared diagnostic; neither D1 policy selection nor later visual acceptance is inferred.

Four initial review concerns are resolved in the final plan:

- Public `segmentAtlasWaterBodies` returns allocated regions, not private clearance cores. One open region means zero or one core; the plan now preserves that ambiguity without a private helper or extra BFS. It uses the public immutable reader, partition and row-weight APIs.
- The complete rooted-graph rule includes exactly one basin per open component, the largest open region being the basin for single/majority, every open region being a basin for multiple, and the declared component-count/90% requirements. These are measurements of returned region indices, not fabricated accepted semantic entities.
- Sampling is full-only. The 30 rows contain two analytic exclusions; 28 eligible inputs have a hard 18-unique-field ceiling only after exact proof of the prescribed ocean, fragmentation and circumference duplicates. The sole permitted metadata exclusions are explicit, and failed rows cannot count as supported duplicates. Inconsistent equality aborts rather than raising the budget.
- Public exact-key parsing, actual status-union vocabulary, source/package/tool trust, fixed artifact inventories and one pass/one computational replay are explicit. Each pass permits at most 37,711,908 scalar evaluations, 28 constructor/placement calls, 18 partitions and 28 requested semantic calls. There is no extra in-pass constructor repeat, preview or rendering.

The global-pole test uses the correct cutoff 715827883 latitude ticks, production integer row weights and zero statistical weight at the poles. BigInt cross-products establish the proposed strict paired ordering without rounded percentages; the secondary latitude moment remains a floating diagnostic. The current local chart stretch has no planet-axis objective, so two passing seeds could not prove general monotonicity.

Ocean modes may change semantic clearance outcomes without changing the scalar mask. A connected continuous complement does not establish a single clearance region. Conversely, fragmentation's presently unused layout band supports a source no-op hypothesis, but output sharing must still prove equality of complete field-affecting inputs, including every island, quota, candidate, certificate and placement frame. The named hull-deficit metric is chart concavity, not spherical coast length or semantic fragmentation; sampling losses and mergers are not rewarded as intended fragmentation.

The full-domain capacity rejection remains the experiment's overall limit even if independent ocean/polar measurements succeed. A future representation, control mechanism, semantic contract or version tuple needs separate review. This plan authorizes none of those changes and cannot settle D1's two-way role correspondence or select the rejected visual family. Final implementation, formatter-idempotence and exact source-manifest clearance still precede any matrix execution.
