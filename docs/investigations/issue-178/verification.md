# Issue 178 — verification and limits

The independently reviewed design checkpoint preceded the private certificate change. The main
task reviewed the focused difference from frozen issue 176: validated far endpoints are returned
by existing collar derivation, and only the explicit new mode calculates and selects the additional
upper witness. Lower width, first disk, extent origin, area, bay and whole-owner gates remain active.
The default path preserves its result schema and arithmetic.

The combined issue-169/170/172/174/176/177/178 suite passes **185 tests in 29 files**, including
17 new focused tests. An independent check matched all **81 saved owner receipts** from the two
issue-172 comparisons and issue-177 r1 in both default and explicit-root modes. The spiral regression demonstrates a shortest opposing-coast chord whose midpoint lies outside
the collar; that distance remains a lower bound, never an upper witness. Supporting and
wedge local successes and failures retain their regression checks as well.

The simple taper evidence reconstructs the frozen source and reproduces old rejection, new-mode
acceptance and exact PNG/SVG bytes. The complete-primary diagnostic repeats nine old/new receipt
pairs and three PNGs against captured source. The final literal passes at all three actual retained
body areas, while initial lobe-ratio/width and smaller-body first-disk failures remain visible.
Independent review corrected the balanced detached fraction from an illustrative `.0095` to the
actual `.016` before authoritative capture. The main task and independent reviewer inspected all
three local images; they establish no full-world visual acceptance.

```sh
corepack pnpm exec vitest run docs/investigations/issue-169 docs/investigations/issue-170 docs/investigations/issue-172 docs/investigations/issue-174 docs/investigations/issue-176 docs/investigations/issue-177 docs/investigations/issue-178
node docs/investigations/issue-178/taper-evidence.mjs
node docs/investigations/issue-178/primary-check.mjs --verify
corepack pnpm exec prettier --check docs/investigations/issue-178
corepack pnpm exec eslint docs/investigations/issue-178
```

Formatting, lint, relative links, public-content and staged whitespace checks accompany the local
commit and repository precommit checks. The prior broad root timeout and successful focused/native
recovery are accurately retained in [issue-168 verification](../issue-168/verification.md). Unchanged
production/native stages are reused; this is not claimed as one clean root `pnpm check` exit.
No production, configuration, dependency, accepted-world or prior runtime source changed. No push.

The guarantee retains binary64 EPS/slack arithmetic rather than formal interval rounding. The far
bound relies on the existing separator class distinguishing R from the entire T/D side, as stated
in [the independent design review](independent-design-review.md). It is not a bound for a different
root-versus-first-disk separator definition. Passing local anatomy still does not prove paid island
placement, all-owner readiness, public controls, world morphology or macOS/Linux equality.
