# Issue 186 — fixed-zero realization discovery

**No production realization policy is selected.** The one declared six-row evaluation and its one computational replay are complete. All 12 coverage checks pass, but preview sampling merges originally separate paid components in every row; three fragmented-row components have no preview land anchors. The restricted zero policy also rejects two zero ticks in the balanced full profile.

This private D1 investigation uses the exact first six rows of [issue184 comparison-r1](../issue-184/comparison-r1/manifest.json), the retained placed geometry and continuous scalar evaluator. It does not generate another constructor, change production code, select a family, or resolve later visual/control concerns.

- [Findings](findings.md) identify the exact failed components and distinguish sampled topology from original geometric certificates.
- [Policy](policy.md) freezes one normalization, Z/H, eight synthetics, actual public boundaries and proposed compatibility implications.
- [Error ledger](error-budget.md) derives conservative local bounds and records the missing two-way role correspondence.
- [Decision](evidence-r1/decision.json), [source manifest](evidence-r1/source-manifest.json) and [completion hashes](evidence-r1/completion.json) retain every outcome.
- [Verification](verification.md) records the completed replay and the read-only hash command.
- [Independent implementation review](independent-implementation-review.md) cleared capture; the [independent retained-result review](independent-result-review.md) confirms the bounded negative result and artifact integrity.

The bounded successor must settle component survival/separation at the accepted sampling profiles and original-role correspondence on extracted boundaries before choosing a policy or version tuple. Passing water coverage alone does not settle either obligation.

The later [formatting repair review](postcapture-format-review.md) documents the exact policy-only transition and the pinned read-only historical verifier; the original evidence bytes remain unchanged.
