# Issue 170 — fixed-input curved-anatomy comparison

This bounded successor follows the [reviewed design](design.md) and the concrete
[issue-169 rejection](../issue-169/visual-review.md). It changes investigation geometry and finite
placement only. Adopted targets, owner quotas, cap separation, old evidence and production remain
unchanged. The maintainer authorized agents, local commits and issue updates with **no Git pushes**.

## Gates and immutable revisions

Complete the independent design review before implementation. Keep cubic-sampled role coastlines
authoritative and calculate their area before stitching/quota fitting; do not alter the final body
behind its role certificates. Record the finite template/curve/site table and placement constants
in source. Run focused tests before any comparison.

The local gate reconstructs all six exact input records from the retained issue-165 `inputs()`
helper twice. Every owner of every input must pass its full applicable certificate before the full
comparison starts. A failed local gate is retained and stops rendering. No alternate seed, owner
relabeling, missing-island shortcut, quota transfer or feature-threshold relaxation may rescue it.

After the targeted local repair, two further failures are directly observable before a world run:
fewer than the three declared layouts survive certification, and the balanced caps violate a
necessary packing bound. The explicit `readiness.mjs` gate preserves these failed thresholds. With
five or more owners, if even the two smallest radii plus the .05 gap require separation above
π/2, every pair would need a negative dot product, which is impossible for five points in three
dimensions (the [packing audit](../issue-169/packing-audit.md) gives the affine-dependence proof).
This is rejection of the constructed radii, not proof that all future anatomy is impossible.
The local gate records both complete role certification and `readyForComparison: false`; no full
image matrix is spent qualifying this known failed candidate. Rejection regression tests preserve
the intended packing/layout thresholds rather than assert that the candidate meets them.

Snapshot source text, hashes and all input controls before the gate. Use a brand-new output directory
inside this issue; never overwrite a prior revision. For each row, construct/place/evaluate twice,
require exact receipt/scalar/owner/mask/PNG equality, and write its receipt immediately. Placement
failure produces a no-proposal receipt and no invented image. Check source hashes again after the
last row so changing implementation during a run cannot silently qualify the result.

## Measurements and visual review

Use the same 400×200 preview, 1600×800 full anchors, and 800×400 review image. Fixed zero remains
the only contour. Sphere-weighted total coverage error must be within .25 percentage points at
both scales; each preview owner remains within .25/count percentage points. Count and distribution
still determine immutable squared-size owner quotas. Every final cap pair must preserve its .05-rad
gap, and local land geometry preserves nominal .05 clearance with no contour motion.

Seam and pole aliases and all 80,400 shared anchors must match exactly. These sampling identities
and continuous certificates do not establish future production contour extraction, island survival,
semantic ocean classification, actual polar bias or cross-platform equality. All six retained inputs
have neutral polar character. Keep those unproved obligations visible in the final disposition.

Review every available native/half image against every positive observation and R1–R6 in the
unchanged [visual contract](../issue-164/visual-contract.md). Inspect actual internal anatomy,
seed/owner diversity, water relationships and localized irregular island groups. Numerical passes
cannot excuse another rounded/interchangeable grammar, smoothing a geometric excavation, or merely
adding edge detail. Count seam-connected pieces together and distinguish polar projection from a
real ribbon. Human decisions remain pending; assistant observations may reject or provisionally
recommend only.

This issue permits one implementation/review pass and one targeted revised comparison at most.
A third comparison, weakened target, second support policy or production change requires a separate
bounded successor. Keep all failed evidence. A provisional recommendation still leaves human visual
selection, accepted ADR, production C1–C3, expanded cohorts and macOS/Linux canonical proof pending.
