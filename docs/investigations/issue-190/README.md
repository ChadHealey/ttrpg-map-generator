# Issue 190 — complete primary gulf prototype

State 1 passes all 60 body-only certificate cases and repeats exactly, but the author and main
reviewer reject its local R3 appearance: a rounded upper projection still opposes a pointed
peninsula. The peninsula supplies the lower bay bank as intended; this mechanism alone has
not removed the open-mouth silhouette. See the [state 1 disposition](disposition-state1.md)
and [verification record](verification.md). The
[independent result review](independent-local-review.md) agrees with the rejection.

The [complete literal design](design.md) received independent
[analytic review](independent-literal-review.md) and [source review](independent-source-review.md)
before the first geometry call. All captured sources remain unchanged. The
[native panel](evidence/state-1/panel.png) and [exact half-size panel](evidence/state-1/panel-half.png)
show the three central paid body quotas at a common chart scale. These are local masks, not
world or family evidence.

Each authorized state permits 60 initial, 60 repeat and 60 computational-replay cases. State 1
spent exactly 180 body-certificate calls. Its computational phases must not run again.
`--verify` performs no useful geometry calls; `--replay` is a one-shot operation that writes a
budget claim and receipt. The [external authority](authority/state-1.json) pins all 15 saved
artifacts. Final tests inspect retained results and pure validators. See the
[harness plan](harness-plan.md).

One final material-state allowance remains, requiring a complete literal design, independent
review and main-task clearance before execution. State 2 is not authorized. No world, island,
placement, production or family acceptance is in scope; existing Large and companion templates
remain unchanged.
