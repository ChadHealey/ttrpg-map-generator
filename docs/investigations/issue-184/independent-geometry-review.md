# Issue 184 — Independent implementation review of local recipe 1

**No actionable implementation/certificate finding in the reviewed first state. Local visual
concerns remain on A and Large, so this is not a recommendation to spend a world matrix without
the root task's local visual gate.** The implementation was written by another agent; this
reviewer supplied the earlier unexecuted geometric hypothesis and independently checked its
translation into code and retained evidence. The root provides a separate local image judgment.

## Source and identity checks

Reviewed [splice.mjs](splice.mjs), the active A/B/Large layouts and their immutable recipe-1
sources against frozen issue-183 A/B, issue-182 Large and issue-172's partition helper.

- Four cubic pieces use the prescribed 4/8/8/4 sampling counts. Exact endpoints are emitted once;
  the retained old samples are reused without numerical reconstruction. The result counts are
  A93, B102 and Large90 coast vertices, within the unchanged180 partition limit and the256
  owner-vertex budget after bounded detached members.
- Cyclic A84→15 and Large81→15 splices preserve the intended free-coast intervals. B30→36
  preserves both adjacent roots; its remapped18→30 lobe wraps correctly in the new coast array.
  Mouth indices4/20 refer to the authored shoulders, not stale predecessor samples.
- All raw attachment polygons, root endpoints, far cuts, collar disks and fixed interior/bay
  witnesses match the predecessors for five anatomy cases and four variations. This preserves
  the raw contract; it does not assert unchanged fitted coordinates after body area changes.
- `remapSites` validates both endpoints and adjacency, rejecting a removed/cross-splice edge.
  All18 central raw site edges are exposed. A/Large retain exact previous site arrays; B uses
  the predeclared old13/16 replacements plus52/54/77/79. Detached payment and compact placement
  still require the common paid-owner lane.
- The first-state literals and exterior shoulder formulas match the declared hypothesis. The
  joins match authored cubic tangent directions; sampled polygon chords are not asserted to
  have exact tangent continuity. These curves remain authoritative polygons subject to the
  unchanged full certificate, without a smooth-curve exemption or post-fit repair.

## Retained evidence replay

Independently ran:

```sh
node docs/investigations/issue-184/local-evidence.mjs --verify recipe-1
pnpm exec vitest run docs/investigations/issue-184/splice.test.mjs
```

The verifier passed all140 retained body-only certificates and reproduced the exact local PNG.
It binds the fixed recipe entries, artifact inventory and writer hash, and compares the complete
saved source closure with the trusted current pinned recipe before executing saved source.
The four focused tests passed. An additional read-only check verified all18 raw sites are body
edges and the complete A/Large site arrays remain exact. No geometry search, new candidate state,
implementation edit or world render was performed by this reviewer.

## Local visual assessment

Inspected [the exact A/B/Large panel](local-diagnostics/recipe-1/panel.png). B's five-vertex
step/V excavation is replaced by a continuous rounded cove. Its depth and two shoulders remain
conspicuous, but the prior angular apex is visibly changed.

A and Large still show long near-horizontal upper bay lips and pointed lower shoulders. Large
particularly reads as a flat-topped wedge in this local view; smoothing the vertex sequence has
not by itself demonstrated resolution of the broader R3 concern. This observation was sent to
the root and implementation author before any common134/world gate. The numerical result and
exact raw-role preservation should be credited without presenting them as visual acceptance.

This review covers recipe1 only. Paid-owner134 readiness, detached islands, world-field receipts,
all nine native/half pairs and any later material state require their own recorded assessment.
No human selection, production readiness or accepted ADR follows from these local results.
