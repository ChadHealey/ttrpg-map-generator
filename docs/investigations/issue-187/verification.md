# Two-state verification

The independent literal design review and main-task clearance preceded construction. The first
actual geometry execution was the complete 60-case capture; no exploratory constructor call or
valid-geometry test was run beforehand. Source text, hashes and the fixed input corpus were
written before geometry import. The same immutable runtime then reproduced its results exactly.

The first state has 60 passing certificates, zero construction errors and two rendered local
panels. It is locally rejected for R3 despite those numeric results. The central four variation
cases coincide by design; these are 60 cases, not 60 distinct shapes.

Executed checks:

```sh
node docs/investigations/issue-187/run.mjs --verify state-1
node docs/investigations/issue-187/stage2.mjs --verify
pnpm exec vitest run docs/investigations/issue-187/*.test.mjs
pnpm exec prettier --check docs/investigations/issue-187
pnpm exec eslint docs/investigations/issue-187
```

All 17 focused tests in four files pass. The original nine tests remain unchanged apart from
the documented test-only correction below. The first-state guarded replay reproduces 60 receipts and two images
against 17 captured source/doc/package files. Tests check the literal table, root/far/site identities,
pre-fit regional formulas, input preservation, uniform fit and exact original quota. Every
retained candidate is re-certified against its stored quota. Negative cases include missing
required bay, wrong quota, malformed anatomy/variation and attempted evidence overwrite.

Provenance regressions reject coherently rehashed injected source and unknown paths before
geometry import, changed PNG bytes before replay and a coherently rehashed false summary
against the actual replay. The native/half test decodes both PNGs and checks every half-size
pixel against its declared even-coordinate native sample.

The initial test run had one test-only expectation error: it requested failure code `quota`
instead of the frozen certificate's actual `quota-residual`. Correcting that expectation did
not change the captured runtime, geometry, predicate, corpus or evidence. The rerun passed.

Node, TypeScript and Prettier versions and the package lock are source-bound. This is local
binary64 evidence, not a formal interval certificate or cross-platform proof.

The final second state also has 60 passing certificates, zero construction errors and two
rendered images, and is independently rejected for R3. Its separate source-bound guard captures
26 source/doc/package files, verifies the completed first state and binds the immutable first
rejection, second literal design, independent review and exact authorization hashes before
executing second-state geometry. No captured first-state source was changed.

Four pure guard tests reject missing prerequisites, incomplete predecessors, changed prerequisite
bytes and invalid authorization outcomes. Four final evidence tests check the exact three-anchor
diff, re-certify all 60 saved cases, replay both states, and reject coherently rehashed review
injection, changed image bytes and false final pass counters. These tests ran only after the
complete final capture. Both image sizes were actually inspected by the author, main task and
independent reviewer. No geometry was repaired after either execution. Repository-wide final checks
and commit remain the main task's boundary.
