# Issue 177 independent runner and evidence review

No actionable evidence-contract or regression finding remains in the reviewed
[runner](run.mjs), [experiment contract](experiment.md) and independent
[verifier](verify.mjs). The experiment is nevertheless **visually rejected**, as
recorded separately in [the complete native/half review](visual-review.md).

The runner supplies the explicit issue-177 constructor to frozen issue-172 local
gate and rendering functions. It creates a new output directory, captures recursive
static and literal dynamic imports plus the retained input JSON and authority
documents, records the complete local gate before rendering, repeats each complete
row exactly, and checks source hashes after execution. The source closure includes
the frozen runner's otherwise unused constructor imports. Earlier sources and
comparisons are not overwritten.

The adopted numeric gate remains total water error within .25 percentage points
at preview and full scales, plus preview-owner error within .25/count percentage
points. Full owner errors remain diagnostics. The three-layout readiness count is
a declared inventory check, not proof of material visual diversity. Seam/pole and
nested-anchor checks retain their explicit scope; they do not prove sampled feature
survival or production extraction.

Independently ran the read-only verifier with `--require-current` on
[local-gate-r1](local-gate-r1/manifest.json) and
[comparison-r1](comparison-r1/manifest.json). Both verify all six exact retained
inputs, **38 captured sources and 68 relative runtime imports**, complete/readiness
and repeat receipt consistency, and current-source equality. The comparison also
verifies all six individual row receipts, coverage arithmetic, reported numeric
eligibility and **12 PNG byte hashes and dimensions**. Every row reports numeric
gates passed. Human decisions remain pending and production remains unselected.

The verifier's eight focused tests pass, including tampered image/source/row/input
cases, omitted and unresolved dynamic dependencies, current-source drift,
preview-owner eligibility versus full-owner diagnostics, and the separate r2 runner
and revision requirement. The reviewed runner passes ESLint; verifier formatting
and lint pass.

Also checked all 17 artifact hashes in each of the retained pre-diversity and final
local panels, and viewed both panels. Their numerical completion does not override
the repeated peninsula and mouth shapes visible at final atlas scales.

This review independently checks proof-to-runner boundaries, source closure,
receipt consistency and image bytes. It does not rerun all scalar grids, constructor
repeats or cross-platform comparisons, and it does not recertify every current
layout geometry. The private bay predicate's separate
[issue-176 review](../issue-176/independent-review.md) remains the relevant
proof-to-code record. No old evidence or runtime source was edited by this review.
