# Issue 184 checkpoints

## First combined local state — rejected visually

The main task inspected the exact
[A/B/Large panel](local-diagnostics/recipe-1/panel.png) and rejected the state:
B's rounded northern cove improves the former step/V, but A and Large retain
long flat lips and opposing pointed shoulders. The independent geometry review
agrees that numerical success alone does not resolve this appearance.
All 140 passing body-only receipts, source files and PNG remain unchanged.
One of three material states is spent; no world matrix has run.

The common private constructor and finite readiness interfaces are prepared
while a second literal hypothesis is reviewed. C and subordinates use unchanged
geometry and seed scopes; the four large candidates are honestly versioned as
`issue-184-large-bay-r1`, because their bay geometry changes. They preserve issue 182's
streams, payments and site/candidate limits without claiming identical fallback
geometry. Constructor unit checks compare unchanged C/subordinate candidates
with their predecessors, including the exact bounded failure receipt when an
individual candidate exhausts its sites.

The 134-input gate requires A/B/C among retained ordinary worlds and layout 3 in
each of default-001/004/006. Missing, substituted, uncertified or unplaced inputs
close the gate. Interface tests use frozen issue-183 successful receipts; they do not
claim184 readiness. No issue-184 134-input run is authorized or recorded at this
checkpoint. See [first local findings](local-findings.md) for actual measured
geometry and reproduction.

## Second combined local state — awaiting visual review

The exact [predeclared second recipe](state-2.md) is captured in
[recipe-2 sources](local-diagnostics/recipe-2/sources.json),
[all 140 receipts](local-diagnostics/recipe-2/report.json), and its
[A, B and Large panel](local-diagnostics/recipe-2/panel.png). All 140 certificates
pass, as do the eleven focused splice, constructor and gate tests. No geometric
constant was adjusted after execution. B and its site ledger remain unchanged;
A and Large use the new declared mouths and bay witnesses while preserving
all raw role geometry and interior witnesses. Both revised constructor versions
now record r2, including the revised large branch.

A and Large have inclined upper banks and retreated shoulders in the local
panel; their projecting lower lips remain a visual concern. At central anatomy,
A/B/Large's largest opening upper bounds are .256047/.267180/.282709 radians,
and depth lower bounds are .216942/.246921/.234357. Full exact metrics remain in
the receipts. Numeric success does not establish a natural-looking margin.

Two of three material local states are now spent. Main-task and independent
visual review precede any full 134-input readiness run; no such run or world
matrix has occurred at this checkpoint. The first state and all its evidence
remain unchanged.

```sh
node docs/investigations/issue-184/local-evidence.mjs --verify recipe-2
```

## Second state — ready for the first contextual comparison

The main task and independent reviewer found a credible upper-bank/shoulder
change, with short opposing lower lips still a concern. All 134 exact paid
construction-and-placement inputs subsequently passed strict repeats. The
ordinary A, B and C layouts and each of the three named large-primary rows
survive. The source-frozen [local checkpoint](local-findings.md) records this
gate and its limitations. Numeric readiness authorizes the bounded world test
only; it does not establish visual acceptance.

## Third and final local state — captured for review

The first nine-row world comparison passed its numerical gates but retained
visual failures. The main task identified A's lower shelf and Large's upper
shelf; the independent review also retained broader B, C and control-row R3
concerns. The exact [third declaration](state-3.md) addresses only the prescribed
A and Large banks. It does not claim to resolve those wider findings.

Before advancing live sources, the first world capture passed its current-source
verification. All thirteen captured local source and document files were then
archived byte-for-byte in [runtime-r1](runtime-r1/manifest.json); the complete
transitive closure remains in the first comparison. The pinned first and second
recipes and every earlier artifact remain unchanged.

The third [source closure](local-diagnostics/recipe-3/sources.json),
[140 receipts](local-diagnostics/recipe-3/report.json), and
[A, B and Large panel](local-diagnostics/recipe-3/panel.png) retain the literal
execution. All 140 local certificates pass, and the read-only verifier reproduces
every receipt and the PNG. Eleven focused splice, constructor and gate tests
pass. No coordinate was adjusted after this execution. The main and large
constructor versions now record r3; their existing seed scopes remain unchanged.

A's lower shelf is visibly reduced in this planar panel, and Large's upper bank
forms a shallow arch. Opposing projecting lips remain a concern. At central
anatomy, the largest A/B/Large opening upper bounds are .256261/.267180/.283980
radians, and depth lower bounds are .225279/.246921/.234759. Full metrics and
binary64 qualifications remain in the receipts. These are body-only local
diagnostics, with no paid-island or whole-world acceptance claim.

The independent geometry review confirms the literal controls, unchanged B and
splice bytes, raw role preservation, and actual exposed site edges. Local review
precedes any third-state run of all 134 paid inputs. All three material states
are now spent; one of two world comparisons has run. There is no fourth state.

```sh
node docs/investigations/issue-184/local-evidence.mjs --verify recipe-3
```

## Final world comparison — numerical pass, family rejected

After local clearance, the frozen third recipe passed all 134 paid inputs and
their 268 strict repeated calls. Ordinary A/B/C and each named recovered large
row survived. The [final comparison](comparison-r2/results.json) then passed all
nine numerical gates. Its read-only current-source verification checks 50
captured sources, 88 relative imports, eighteen images, 134 condensed entries
and the predecessor manifest/results binding, with no source drift.

The main task's [complete final image review](visual-review-r2.md) rejects the
family. Large's former upper shelf is materially improved, notably in
default-004, but A's long opposing lips in normal-01 and repeated B/C/control
mouths remain. The main task is stricter on several unchanged layouts in the
complete final cohort; this revised judgment is not an implementation regression
in unchanged B/C source. The [independent final review](independent-world-review-r2.md)
agrees on every final row disposition: normal-01/02/03 and both controls fail R3;
normal-04 and default-001/004/006 provisionally pass with concerns. Both reviewers
clear the specific former Large shelf finding without selecting the family.
Neither assistant review constitutes human selection.

The focused issue suite passes 25 tests. The main task's combined investigation
run passes 313 tests in 53 files, with one test in one additional file skipped.
These checks do not establish a clean root `pnpm check` exit or overturn the
visual rejection. All three material local states and both matrices are spent;
there is no fourth state or third matrix. The captured declarations, runtime,
prior source archive and every local/world artifact remain unchanged. Any
further body-and-bay construction hypothesis requires separate bounded scope.
