# Issue 115 real-owner focus handoff

This record owns the exactly one successor focus qualification authorized by
live issue #115. It starts from exact integrated commit `14ba656` and preserves
issue #114 and every earlier evidence record exactly. It changes no product,
controller, observer, fixture, workload, sampler, timeout, schema, release
protocol, or downstream issue #104 authority.

## One-shot authority

Immediately before dispatch, the repository owner confirmed: “OK, I am here
now for a while. Lets try 114 again.” This fresh confirmation establishes that
the owner is at the unlocked Mac and authorizes exactly one issue #115 attempt
with exactly one physical focus click. It does not reopen issue #114 and does
not authorize a retry or fallback.

After the issue-authorized quick checks pass from a clean committed boundary,
the unchanged issue #110 controller may launch the exact preserved packaged
candidate once with one declared real-owner focus action and its unchanged
120,000-ms durable marker handoff. The coordinator directly validates the
owner-only marker and then immediately instructs the owner to click exactly
once on the packaged TTRPG Map Generator window or its unambiguous Dock icon
and do nothing else. The controller independently decides whether the exact
candidate becomes Accessibility and Workspace frontmost, has a supported
positive frame, retains identity through the permitted raise, and passes the
canonical independent observer.

Codex performs no GUI input, Computer Use, AppleScript, synthesized event, Dock
scripting, or product action. The owner performs no product-control click,
typing, scrolling, menu action, or second focus action. No fixture, sampler,
artifact, destination, dispatch, measurement, issue #95 operation, or issue
#104 activity is permitted.

Timeout, absent click, duplicate action, wrong target, ambiguity, focus loss,
observer disagreement, identity drift, marker failure, or cleanup failure stops
the qualification as invalid with no retry or fallback. A valid result allows
issue #104 to resume only in a separate fresh Sol/high task.

## Reused gates and focused checks

Issue #114's immediately preceding passing seven Swift suites, 14 packaged
dispatch tests, package build, and root check are reused without rerun unless
drift is detected. Before target launch, issue #115 revalidates only exact clean
HEAD, zero target processes and marker collision, the six canonical candidate/
controller/observer/sampler/retention SHA-256 identities, unchanged production/
fixture/protected-evidence surfaces, and public privacy and diff checks.

Public evidence contains no marker path or token, local path, PID, service
identifier, username, raw diagnostic, pixel, screenshot, Accessibility content,
credential, or private artifact location. The terminal result is recorded only
after direct marker cleanup and zero-target-process checks.

## Qualification state

The issue-authorized quick checks passed from clean coordination commit
`39b5f45`. Its parent was exact integrated commit `14ba656`; zero target
processes and no exact marker collision existed; the candidate, controller,
readiness observer, cancellation observer, RSS sampler, and retention utility
reproduced all six canonical SHA-256 identities. The only change from the
integrated base was this docs-only coordination record. Production, fixture,
protected-observer, and prior-evidence surfaces were unchanged, and the public
privacy and diff checks passed. No drift was detected, so issue #114's
immediately preceding passing seven Swift suites, 14 dispatch tests, package
build, and root check were reused without rerun.

**INVALID — PRE-HANDOFF PRE-DISPATCH STOP.** The unchanged controller was
invoked exactly once and launched the exact packaged candidate. It established
one fresh exact application and Accessibility window, but stopped before marker
publication because a distinct initial foreground application was not available
for the operator handoff. The controller reported the sanitized `foreground`
invalidation and terminated its candidate.

Because the owner-only marker was never published, direct marker validation was
not reachable and no `CLICK NOW:` instruction was emitted. No owner action was
requested. The controller performed no focus observation, raise, frontmost
write, independent-observer run, fixture configuration, sampler start, artifact
or destination creation, dispatch, measurement, issue #95 operation, or issue
#104 activity.

The controller's unowned marker cleanup completed successfully. Separate
terminal checks found the marker and publication temporary absent and zero TTRPG
processes. No retry or fallback ran. Issue #114 remains immutable. Issue #104
remains blocked and **UNCONSUMED**; another attempt requires a new explicit owner
decision. The complete sanitized receipt is in
[`qualification-2026-08-25/raw-results.json`](qualification-2026-08-25/raw-results.json).
