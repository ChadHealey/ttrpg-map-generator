# Issue 179 independent runner and evidence review

No actionable runner, receipt-contract or regression finding remains in the reviewed
[runner](run.mjs), [experiment](experiment.md) and [verifier](verify.mjs). The
[independent visual review](visual-review.md) records a mixed result and withholds
a family recommendation because two ordinary rows still fail provisionally.

The runner passes the explicit issue-179 constructor into frozen issue-172 local
gate and render functions. It preserves the existing complete-cohort, ordinary
layout-inventory and necessary packing checks, immutable output directories,
per-row exact repeats and source-integrity checks. Recursive source capture includes
static and literal dynamic imports, even the frozen runner's unused constructor
imports, plus retained input JSON and the issue-178 design authority.

The new verifier privately retains the issue-177 checks and requires every accepted
owner certificate to record `collarWidthUpperMode: 'root-and-far'`. Missing or root
fallback modes reject. This records the declared constructor policy without
recertifying placed geometry through a different mode. The fixed-zero field,
coverage limits, dimensions, seam/pole and nested-anchor checks remain unchanged.
Full-owner coverage errors remain diagnostics; only preview-owner errors receive
the adopted .25/count percentage-point gate.

Independently ran the read-only verifier with `--require-current` on
[local-gate-r1](local-gate-r1/manifest.json) and
[comparison-r1](comparison-r1/manifest.json). Both pass for all six exact retained
inputs, **38 sources and 68 relative runtime imports**, current-source equality,
complete/readiness consistency and the new mode requirement. The comparison also
passes all individual row, coverage arithmetic, numeric-stage and **12 PNG hash
and dimension** checks. Every row reports numeric gates passed. Human decisions
remain pending and production remains unselected.

The verifier's **nine focused tests pass**, covering tampered images, source text,
missing dependency closure, dynamic imports, changed rows/inputs, repeat/readiness
claims, current-source drift, preview/full owner-error semantics, the separate r2
runner/revision contract and missing/fallback certificate modes. Verifier lint and
formatting pass. No earlier verifier or evidence source was modified.

The final-c3 local panel's **17 artifact hashes match**. Its panel was inspected,
along with the earlier requested A/B/C local views. All six final native images and
all six half-size images were then inspected separately; the mixed visual verdict
comes from those final world images, not from the local panels or numeric metadata.

This verifier establishes source closure, receipt consistency and PNG bytes. It
does not independently rerun all constructor repeats, scalar grids, production
extraction or cross-platform comparisons. The separate
[issue-178 implementation review](../issue-178/independent-implementation-review.md)
remains the proof-to-code record for the tighter width witness. Inventory labels
alone do not establish material visual diversity, and this review supplies no
human selection or production adoption decision.
