# Immutable two-fixture harness

Exactly two fixed inputs, `peninsula-bank` then `lobe-bank`, execute the complete literals in
design.md. Both use primary false, no islands and their own surviving polygon-area quota.
There is one capture, no repair state, no search and no anatomical variation. The useful
fixture builder must not execute in any test or informal call before reviewed-source clearance.

The entry captures the entire static relative-import closure rooted at fixtures.mjs,
evaluate.mjs and corpus.mjs. Runtime imports permit investigation .mjs files and node builtins
only; TypeScript's parser rejects dynamic import and import.meta in that closure. In addition,
source capture binds sources.mjs, run.mjs, design.md, harness-plan.md, the independent design and source
reviews, package.json and pnpm-lock.yaml, with Node/TypeScript/Prettier versions. The closure
includes frozen 178, 176 and 169 dependencies and the new private certificate, topology,
partition and wedge helpers when imported by the runtime. All captured text must be formatter
idempotent before the source freeze.

Exclusive recording writes the complete source manifest, compressed exact source text and
fixed corpus before the first useful builder import. It runs both cases, retains construction
errors and every certificate failure, repeats the full result exactly, then writes compressed
reports, summary, native panel, half panel and a completion receipt. Source text is checked
again before completion. Existing output directories and unknown capture names reject.

The unlabelled panel order is peninsula bank, lobe bank. Native dimensions are 640 by 320,
with two 320-pixel cells and common LAEA chart span 1.8 in both axes; the half panel is 320 by
160, using native pixels (2x,2y). The palette and project-owned PNG codec match issue 187.
These literal polygon component masks are local diagnostics, not world renders or complete
primary silhouettes. There are no additional thumbnails or geometry images.

Read-only replay checks exact file inventory, complete manifest, trusted CURRENT source closure
and exact source text before loading anything retained. A coherently rehashed manifest cannot
authorize altered code. The in-memory trusted closure is loaded only after that match; results
and both PNGs are regenerated twice and compared byte for byte. Tests must reject tampered
source before execution, malformed paths/inventory, changed images and coherently false summary
counts. No verification command writes output. All captures remain immutable on failure.
