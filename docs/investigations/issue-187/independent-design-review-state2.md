# Issue 187 independent final-state design and guard review

**Clear the declared second state for the fixed 60-case local experiment.** The changed literals
address the specific terminal lower-jaw hypothesis without an indexing or analytic contradiction
found in this review. This is the last permitted material state. No second-state constructor,
sampler, certificate or renderer was executed during this review.

## Literal and displacement checks

The complete second table changes only anchors 26, 27 and 28 to (.300,-.550), (.800,-.500)
and (.650,-.350). Role indices, far indices, disks, bay interval, witnesses, regional displacement
formulas and unexecuted site identities remain explicitly fixed. The lobe1 root end is therefore
an actually moved point at sample81; retaining its index does not preserve the old root geometry
or area shares. Both far endpoints remain interior vertices of the same declared role path.
All role intervals remain disjoint, and the bay still belongs to B's index complement.

For the exact tension .12 sampler, the two cubic x-control sequences are
[.650,.6128,.5272,.490] and [.490,.4528,.3772,.340]. Both strictly decrease, and no listed
regional displacement changes their x coordinates. Thus both true cubics and their sampled
points continue in x through the mouth over the whole u,v box. This removes a local x-turn;
it does not guarantee that a projected mouth or the new southeast end will look acceptable.

The y-control displacement coefficients for anchor28→mouth0 are
[.008,.008,.00824,.008], and those for mouth0→anchor1 are
[.008,.00776,.00696,.006]. Consequently, over v∈[-1,1], their minimum successive y increments
are respectively [.0336,.07216,.02376] and [.02376,.0224,.02184], all positive. The claimed
central y monotonicity therefore extends to the complete declared displacement box.

The mouth determinant and upper-shoulder conditions are unchanged from the first independent
review. The new exterior tangent gives det(a,va)=-.02980-.00150v. More importantly, substitution
into the actual sample86 incident-chord formula gives approximately
-.0071382222-.0003457185v. Its least negative value is below -.00679. Thus the actual lower
incident sampled segment still leaves the structural shoulder through an excluding radial face;
the proof does not rely only on an initial spline tangent.

These are real-arithmetic design checks with clear margins. All other sampled edges, the expanded
water wedge, first disks, role/root topology, chart cap, quotas and numeric margins remain actual
frozen178 certificate obligations. The broader southeast region can change the fit and every
credited share. Neither its geometry nor any passing first-state certificate establishes that
these obligations will pass in state2. The fixed three paid-body quota expressions and single
uniform fit remain appropriate; islands=[] proves no detached payment or site feasibility.

## Separate prerequisite guard

I reviewed `stage2.mjs`, its guard plan and pure boundary tests. It reads a fixed prerequisite
inventory and requires an exact declaration binding the guard source, complete second design,
this independent review, the frozen first rejection/review and first manifest/receipt/summary.
The declaration explicitly states rejected-local-R3 and fixed-60-case design clearance. It then
runs the complete first-state read-only verifier before loading new geometry. A completion flag
alone does not authorize the predecessor.

The guard captures its own source, all prerequisite text, the new layout's transitive runtime,
shared evaluator/corpus and original authority documents. It writes those bytes before importing
the second geometry, uses exclusive output creation, checks source stability before and after
execution, and retains exact repeats and complete output receipts. Its distinct manifest and
receipt revision prevents output from the historical unguarded state2 entry from passing this
new verifier. Keeping that historical source unchanged preserves the first capture.

I independently ran **4 pure guard tests**, covering missing prerequisites, incomplete or wrong
predecessors even after rehashing, stale design/review/source hashes, and substituted or extra
authorization fields. These tests imported no second-state geometry. No additional blocker was
found in the guard. Final authorization must hash the final literal design and this review;
changing either afterward invalidates the declaration.

## Scope of clearance

Proceed only after the main task's final source/declaration check. Retain all 60 outcomes and
both predeclared center-quota panel sizes, including numeric or visual failures. The same local
panel scale enables comparison with state1; no extra thumbnails or hidden intermediate geometry
are authorized. This review supplies no numeric or visual pass in advance.

A surviving opposing mouth, angular extended tip or other failed local relationship rejects
this final state. There is no third state, world matrix or 134-input sweep under this issue.
Whole-family construction, detached owners/placement, public controls, production realization,
platform equality, ADR and human acceptance remain separate gates.
