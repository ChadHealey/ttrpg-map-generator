# Issue 183 bounded bay revision

This private experiment addresses the remaining balanced-control R3 finding in
[issue-179 r2](../issue-179/visual-review-r2.md), with the frozen
[issue-182 large-primary extension](../issue-182/templates.mjs). The unchanged
[issue-164 visual contract](../issue-164/visual-contract.md) and
[issue-167 targets](../issue-167/README.md) remain authoritative. No production,
semantic control or field change is authorized. All three material local states
are spent at this freeze; the final state is recipe3.

At most three material combined local bay states and two complete nine-row world
comparisons are permitted. State means the complete named A/B/C bay recipe, not
three independent per-layout tuning allowances. Every material state retains its
source, all 45 body-size/anatomy-corner receipts, and a paired planar panel before
another state is considered. These planar images are local diagnostics, not
world or human acceptance evidence.

Exact owner/placement receipts identify the previous upper-middle balanced body
as B/owner-3 and lower-center body as A/owner-2. C/owner-4 is the upper-left body,
whose blunt compact recess was also inspected. The main task corrected its
initial preserve-A assumption on this evidence and authorized bay-only A edits.
All role intervals, roots, far cuts, collar disks, lobe/peninsula coast geometry
are preserved before fitting. The third state explicitly moves B's prefit
interior witness because of the relocated bay; that witness change does not
relabel any feature. Bay-adjacent periodic
coast tangents may change with the declared bay anchors; distant anatomy may
not. Uniform fitting then pays exactly the original body quota.

The constructor privately revises candidates 0–11 using copies of the former
base constructor; candidates 12–15 delegate directly to frozen182. No random
scope changes, owner deletion, quota change, category payment, site budget or
extra fallback is allowed. A subordinate remains exactly the previous geometry.
Ocean/fragmentation/polar limitations from [issue-180](../issue-180/findings.md)
remain unresolved and explicit.

Before each permitted matrix, all six original inputs and all128 exact
additional-default inputs must actually certify and place, with exact repeats,
full owner sets and ordinary layouts A/B/C present. The [gate](gate.mjs) runs the
independent paid-quota/receipt/certificate/placement auditor, not just a cap
estimate. One failed input closes the gate. The local source reviewer and main
task must inspect the retained panel before a world run; numeric readiness
alone does not authorize it.

The [fixed nine inputs](corpus.mjs) are the six unchanged issue-179 inputs plus
issue-180 default-001, default-004 and default-006. No seed can be replaced.
Each matrix snapshots the transitive runtime/input closure before rendering,
retains every row immediately and checks exact repeat data/images plus final
source integrity. Existing 400×200 preview,1600×800 native and800×400 half images,
coverage/owner tolerances, zero contour, seam/pole/shared anchors, guard gap and
binary64 certificate qualifications are unchanged. No raster is run for128 seeds.

Both main task and independent reviewer must inspect all18 images against each
positive and R1–R6. A provisional recommendation requires all seven ordinary
rows to pass and both controls to remain interpretable. Human selection,
production qualification, other control realization and cross-platform proof
remain pending. See [local findings](local-findings.md) for the current disposition.
