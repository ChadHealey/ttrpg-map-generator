# Issue 184 verification scope

The focused issue-184 suite passes 25 tests in five files. It covers cyclic
splice identity and rejected removed references, actual curved samples, exact
raw role and site preservation, unchanged C/subordinate outcomes, input bounds,
closed readiness gates, both retained paid readiness captures, and the world
verifier's provenance and failure paths. The main task's combined investigation
suite passes 313 tests in 53 files, with one test in one additional file skipped;
the 25 issue-specific tests are included. The skipped historical issue-152 suite
requires its separately supplied `ISSUE_152_SOURCE_DIR`; no new test is skipped.
This is not a new complete root
`pnpm check` result.

Each of the three immutable local recipes retains 140 body-only certificates
and one planar panel. All certificates pass. Their read-only local verifiers
check pinned source bytes before executing captured code and reproduce all
receipts and PNG bytes. The first local state was rejected visually and never
ran the paid readiness corpus. States 2 and 3 each retain all 134 paid inputs:
six retained worlds and 128 exact additional defaults. Each readiness replay
verifies 268 strict repeated calls, complete source and artifact inventories,
all quotas/certificates/placements and every retained failure receipt. Neither
readiness run rasterizes those 128 additional seeds.

The final [state-3 gate](readiness-result-r3.json) has no failures. It retains
ordinary layouts A/B/C and actual large layout 3 in each of default-001,
default-004 and default-006. Failed intermediate candidate/site and placement
attempts remain visible. The six live geometry/constructor modules were checked
against their immutable recipe-3 copies, allowing only relative import rebinding.

## World evidence and source chronology

The first comparison's nine numerical rows, 18 PNGs, condensed 134-entry gate,
48 captured sources and 88 relative runtime imports verify. Before state 3,
it also passed `--require-current`. Thirteen local source/document files were
then [archived byte-for-byte](runtime-r1/manifest.json), with the full transitive
closure still in the original world capture. Archive hashes remain verified.

After the authorized state-3 advancement, historical verification still passes
but correctly reports four changed current sources: `templates.mjs`,
`base-templates.mjs`, `layout-large.mjs` and `layout-a.mjs`. This is expected
current-source drift from a retained historical recipe, not a change to its
captured bytes. Use ordinary verification for that capture; requiring current
equality now would correctly fail. Pinned local and readiness recipes remain
separately replayable after live advancement.

The final comparison passes all nine numerical rows. The main task's read-only
`--require-current` check verifies 50 captured sources, 88 relative imports,
eighteen images, 134 condensed gate entries and predecessor manifest/results
binding, with no current-source drift. Its predecessor binding does not replace
separately verifying the first capture's images. The main task inspected all
eighteen images and [rejects the family](visual-review-r2.md), while recognizing
Large's materially improved shelf. A's normal-01 lips and repeated B/C/control
mouths remain. The [independent final review](independent-world-review-r2.md)
also inspects every image and agrees on all final row dispositions. Both reviews
retain provisional passes for the three recovered large-primary rows, without
inferring complete-family or human acceptance.

The world verifier checks source text, import closure, exact input/receipt
structure, coverage arithmetic, condensed gate consistency and PNG bytes. It
does not independently rerender scalar grids, recertify condensed gate entries,
repeat the constructor or establish visual acceptance. Those claims require
their separate retained readiness evidence and actual image review. Numerical
success cannot override either comparison's visual failures.

Run from the repository root:

```sh
corepack pnpm exec vitest run docs/investigations/issue-184
corepack pnpm exec vitest run docs/investigations
node docs/investigations/issue-184/local-evidence.mjs --verify recipe-1
node docs/investigations/issue-184/local-evidence.mjs --verify recipe-2
node docs/investigations/issue-184/local-evidence.mjs --verify recipe-3
node docs/investigations/issue-184/readiness.mjs --verify recipe-2
node docs/investigations/issue-184/readiness.mjs --verify recipe-3
node docs/investigations/issue-184/verify-world.mjs docs/investigations/issue-184/comparison-r1
```

Verify the final comparison read-only with:

```sh
node docs/investigations/issue-184/verify-world.mjs docs/investigations/issue-184/comparison-r2 --require-current
```

Do not rerun historical writers or the renderer against retained directories.
All three local states and both world comparisons are spent. Existing targets,
preview/full tolerances, zero contour and sampled
seam/pole/shared-anchor checks remain unchanged. Human selection, production
extraction, semantic control coverage and cross-platform proof are not supplied
by these private experiments.

Formatting, lint, public-content and relative-link checks accompany the focused
work. The unchanged broad production/native stages retain the earlier
[timeout and focused/native recovery record](../issue-168/verification.md), also
summarized in [issue-179 verification](../issue-179/verification.md). No new clean
root-check exit is claimed. No production, dependency, accepted data or earlier
investigation source was changed. No push.
