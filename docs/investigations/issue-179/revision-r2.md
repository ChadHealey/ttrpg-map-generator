# Issue 179 — second and final comparison

The [first comparison](comparison-r1/results.json) passes all numeric gates but its
[independent visual review](visual-review.md) rejects ordinary normal-01/02 and the balanced
control for repeated slot-like bays. The main task's native/half review agrees. Both ordinary
failures prominently expose layout A; the other two ordinary rows are provisionally acceptable
with concerns. Broad-root peninsulas are a real improvement over issue 177.

This revision changes only A's declared bay coast and mouth into an oblique, asymmetric opening.
It preserves the three literal layout families, all r1 random scope strings, B/C and subordinate
coasts, detached-member choices, public controls, exact quota formulas, certificate, placement and
field semantics. A is uniformly refitted to its unchanged quota; resulting guard or placement
changes are measured rather than concealed. Revision metadata changes separately from randomness.

The fixed bay witness remains declared before fitting, with the same wedge proof and all adopted
opening, depth, area, disk, width and role constraints. No cosmetic field noise, changed input,
owner deletion or post-fit feature choice is used. Local failures are retained before full
six-owner-set readiness is checked again.

The new [runner](run-r2.mjs) explicitly selects [templates-r2.mjs](templates-r2.mjs). It captures its
own full source closure, the r1 runner and this revision record before execution, while all r1
runtime/evidence remain frozen. The existing r1 experiment contract, thresholds, repeat checks
and independent native/half review apply unchanged. This is the second and final permitted world
matrix under issue 179; no third run or production acceptance is inferred.
