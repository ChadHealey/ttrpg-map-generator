# Issue 181 — independent source and contract review

The main task independently reviewed the four authored documents against current core control
ownership, generator parameter factories and metadata, strict persistence DTOs, the production
quantizer/contour type, water segmentation policy and owning M2/fixture documents. No actionable
contract or compatibility finding remains after the corrections below.

The proposal correctly identifies a real input-boundary change: quota-first geometry consumes
water before classification, while legacy macro parameters exclude it. An additive v3 parameter
variant and version-aware invalidation are required; changing the global legacy root would be a
regression. The draft preserves generator-free reopening, old version pairs and strict rejection
of mixed provenance without claiming that any of this new code exists.

The current contour type accepts odd doubled ticks; zero cannot pass through it unchanged.
The separately versioned zero hypothesis therefore needs a precise tie/extraction contract.
The proposal accurately distinguishes the private raster dimensions and million-tick rounding
from production profiles and ties-away quantization. Its D1 gate is an unresolved numerical and
compatibility question, not a request for permission merely because implementation is absent.

The reviewed documents also distinguish continuous raw-water connectivity from the production
clearance-core marine policy. Connected raw water does not prove that semantic multiple basins
are impossible. The independently derived owner-cap obstruction applies specifically to the
frozen 1.4-radian chart family; it does not disprove the adopted feature targets generally.

Review corrected a paraphrase presented as a direct visual-contract quotation. It also restored
the conditional status of a proposed manifest-version change: generator/behavior 3 must be
truthful, but a manifest format must not be bumped solely because its declared generator changes.
The exact manifest identity belongs in the final accepted version contract.

The child scopes remain conditional drafts. They require fresh readiness/context estimates when
authored as executable implementation issues. Human selection, an accepted successor ADR, the
production 128-seed sweep, complete 12-default/6-control cohort and macOS/Linux evidence remain
unmet. This review accepts the documentation discovery, not a production family or ADR.

Formatting, relative links, public-content and repository precommit checks accompany the local
commit. Production, accepted records, dependencies and historical evidence are unchanged; prior
broad validation and its disclosed timeout/recovery are retained in
[issue 168 verification](../issue-168/verification.md). No push.
