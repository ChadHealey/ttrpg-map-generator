# Issue 183 independent local checkpoint review

**Recipe 2 remains visually unpersuasive at the local checkpoint despite passing its numeric
checks.** This review covers the baseline and first two combined material recipes only. No
world matrix or human acceptance is claimed, and the review does not authorize further tuning.

## Actual local images

The exact [baseline](local-diagnostics/baseline/panel.png),
[recipe 1](local-diagnostics/recipe-1/panel.png) and
[recipe 2](local-diagnostics/recipe-2/panel.png) panels were independently inspected. Each shows
A, B and C at the same declared large paid-body quota, without detached islands or spherical
placement. These local views cannot establish whole-atlas arrangement or half-size readability.

The baseline has three distinguishable broad interiors, with the compact right-facing mouths
identified in the earlier issue-179 review. Recipe 1 changes those margins, but all three bodies
still show conspicuous compact hooked recesses. Their similar placement and scale make that
shared feature especially obvious side by side. I agree with the root's local rejection; the
45 passing certificates do not resolve this visual concern.

Recipe 2 gives A and C straighter oblique lower margins and more angular openings. B's source
and compact hook are unchanged from recipe 1. The panel now reads as two sharp wedge-shaped
notches and one hook on otherwise broad bodies. The internal lobe/taper hierarchy remains,
but the requested distinct broad marginal recesses are not yet persuasive. This is a concrete
remaining R3 concern about the visible angular or hooked cuts, not rejection merely because
a bay exists. No circle-subtraction or support-clipping implementation is inferred.

My local recommendation is to withhold clearance for a world matrix on recipe 2. A numerical
pass is useful evidence of feasibility, but does not establish resolution of the targeted
balanced-control silhouette failure. Any next step remains subject to the already declared
combined-recipe and matrix allowances.

## Constructor and provenance review

The private base constructor differs from frozen issue-179 in its A/B/C imports and revision.
Its quota formula, subordinate coasts, island payments, seed scopes and finite site search
remain. The wrapper uses those revised candidates for indices 0–11 and delegates indices
12–15 to frozen issue-182. The sixteen-candidate bound, unchanged certificate, placement and
field remain explicit. Tests compare raw attachment polygons, roots/far cuts and interior
witnesses before fitting; the declared bay changes do not relabel other anatomy afterward.

Independent review flagged recipe 1's inherited `frozenCandidateCount: 12` descriptor as
misleading after the first twelve bay candidates were revised. Recipe 2 correctly records
`revisedBaseCandidateCount: 12` and `frozenFallbackCandidateCount: 4`, with the separate
issue-183 base revision and retained issue-182 fallback revision. Recipe 1's historical source
and receipts remain intact rather than being silently rewritten.

Per-recipe source directories preserve the exact runtime selected for each readiness capture.
At the recipe-2 checkpoint, all five working constructor/layout sources matched their captured
recipe-2 copies, allowing only the required relative-import prefix difference. Later working
edits must not be mistaken for those immutable versions.

The local verifier validates fixed entries and artifact inventory, checks writer/hash records,
and compares the entire saved source map with the trusted current per-recipe closure before
executing saved code. The readiness verifier likewise validates exact source inventories,
fixed dependencies, current closure and frozen authority before replay. Manifest-controlled
source hashes alone are not the authority to execute a different recipe.

## Independent verification

Ran the read-only local verifier for baseline, recipe 1 and recipe 2. Each reproduces all
45 certificate reports and its exact panel PNG. Ran both readiness verifiers successfully:

```sh
node docs/investigations/issue-183/readiness.mjs --verify recipe-1
node docs/investigations/issue-183/readiness.mjs --verify recipe-2
```

Each verifies the exact retained six inputs plus the exact 128 additional defaults, all
134 construction-and-placement passes, and 268 strict repeated calls. Each capture has
28 source records with 17 frozen issue-179 runtime matches. All receipt/artifact/source
checks pass; prior failed attempts remain in the successful results. No raster is evaluated
by this readiness lane.

Independently ran the five focused constructor tests successfully at recipe 2. They verify
the predeclared nine future comparison inputs, raw non-bay role preservation, exact frozen
fallback outputs/streams, all 45 local certificates, all 134 actual paid owner sets and the
three retained ordinary layout IDs. Inventory labels alone do not prove visual diversity.

No actionable constructor or evidence-contract finding remains at this checkpoint. The
outstanding concern is the local silhouette result. Ocean, polar and fragmentation semantics,
other control-domain limitations, production extraction, cross-platform equality and human
selection remain outside these numerical and local-image checks.
