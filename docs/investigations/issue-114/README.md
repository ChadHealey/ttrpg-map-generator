# Issue 114 real-owner focus qualification

This record owns the exactly one real-owner focus qualification authorized by
live issue #114. It starts from exact integrated commit `cdea68b` and changes no
product, controller, observer, fixture, workload, sampler, timeout, schema,
release protocol, or downstream issue #104 authority.

## One-shot authority

The repository owner freshly confirmed immediately before this task that the
Mac is unlocked in the active console session and that they are present to
perform exactly one click during the controller's 120,000-ms handoff. That
confirmation applies only to this task and this sole attempt.

After every bounded issue #114 gate passes from a clean committed boundary, the
unchanged issue #110 controller may launch the exact preserved candidate once
with one declared owner focus action. The coordinator directly validates the
owner-only durable marker before asking the owner to click exactly once on the
packaged TTRPG Map Generator window or its Dock icon and then do nothing else.
The unchanged controller independently decides whether the exact-candidate
Accessibility, Workspace, positive-frame, retained-identity, raise, and
independent-observer predicates pass.

Codex performs no GUI input, Computer Use, AppleScript, synthesized event, Dock
scripting, or product action. The owner performs no product-control click,
typing, scrolling, menu action, or second focus action. No fixture, sampler,
artifact, destination, dispatch, measurement, issue #95 operation, or issue
#104 activity is permitted.

Timeout, no click, duplicate action, wrong target, ambiguity, session or
identity drift, focus loss, observer disagreement, marker failure, or cleanup
failure stops the qualification with no retry or fallback. The terminal record
is written only after direct marker and process cleanup checks.

## Evidence and privacy boundary

Issue #113's exact-base full-gate evidence is reused. Issue #114 reruns only the
clean-head and collision checks, exact canonical identities, focused issue #110
latch suite and six predecessor Swift suites, 14 packaged dispatch tests, and
privacy, authorized-surface, fixture/production-surface, protected-evidence,
and diff gates named by the live issue.

Public evidence contains no marker path or token, local path, PID, service
identifier, username, raw diagnostic, pixel, screenshot, Accessibility text,
credential, or private artifact location. Prior issue #109–#113 evidence stays
immutable. Issue #104 remains **UNCONSUMED** unless this qualification is valid
and a later fresh Sol/high task is separately authorized to resume it.
